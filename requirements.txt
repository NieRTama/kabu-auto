# 依存はすべてバージョン固定（再現性確保・P2-7）。
# yfinance のみ、Yahoo側のエンドポイント変更に追随できるよう「メジャー固定の範囲指定」とする
# （完全固定すると古い版がYahoo仕様変更で取得不能になるため。動作確認版は 1.4.1）。
requests==2.31.0
yfinance>=1.4.1,<2.0.0
pytz==2026.2
websocket-client==1.7.0
pandas==2.1.4
numpy==1.26.2
scikit-learn==1.3.2
lightgbm==4.1.0
sqlalchemy==2.0.23
fastapi==0.104.1
uvicorn[standard]==0.24.0
APScheduler==3.10.4
loguru==0.7.2
pydantic==2.5.2
pydantic-settings==2.1.0
python-dotenv==1.0.0
PyYAML==6.0.1
httpx==0.25.2
tzdata==2026.2  # Windowsで zoneinfo(Asia/Tokyo) を使うために必要。tzルール更新時は適宜引き上げる
tweepy==4.14.0  # X（旧Twitter）への日次レポート投稿用（任意機能。x.enabled=falseなら未使用）
