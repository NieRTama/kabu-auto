# kabu-auto 概要設計書

| 項目 | 内容 |
|------|------|
| システム名 | kabu-auto |
| バージョン | 1.13.1.b |
| 作成日 | 2026-06-16 |
| 最終更新 | 2026-09-04 |
| 対象環境 | Windows ノートPC（24/365稼働） |

### バージョン採番ルール

`メジャー.中規模.小規模.軽微` の4区分で管理する。

| 区分 | 位置 | 規則 | 例 |
|------|------|------|----|
| メインリリース | 1桁目 | 製品としての大規模リリース。当面 `1` 固定 | **1**.x.x.x |
| 中規模な改修 | 2桁目 | 新サブシステム・主要機能の追加（バックテスト・認証等）ごとに +1 | 1.**7**.x.x |
| 小規模な改修 | 3桁目 | 既存機能の拡張・補助機能の追加ごとに +1 | 1.7.**5**.x |
| 軽微な改修 | 4桁目 | バグ修正・ドキュメント整合・CI修正・設定微調整ごとに a→b→c…と更新 | 1.7.5.**c** |

> 軽微（4桁目）は上位区分（中規模・小規模）が上がるたびに `a` にリセットする。
> 例：メインリリース1回・中規模7回・小規模5回・直近の小規模以降に軽微3回 → `1.7.5.c`。

### 修正履歴

新しい順。区分の丸数字は採番上の通し番号（中規模①〜⑫／小規模①〜⑩）。

| バージョン | 日付 | 区分 | 内容 |
|---|---|---|---|
| **1.13.1.b** | 2026-09-04 | 軽微 | **セクター集中率のデッドロックを解消**：集中率の分母が「投資済み額」で買付余力を含んでいなかったため、保有が9432の17,260円だけの状態では `147,500 ÷ (17,260 + 147,500) = 89.5%` となり上限55%を超えて全銘柄が却下されていた。**保有が少ないほど必ず超過し、保有ゼロなら比率は常に100%**で、上限比率を上げても解決しない（システムは起動以来一度も自力で最初の1銘柄を買えていなかった）。分母を**総資金（買付余力＋建玉評価額）**に変更し、この上限本来の意図「資金のN%以上を1セクターに集中させない」と実装を一致させた。余力未取得時は従来の厳しい判定へフォールバックする。却下理由に金額と比率を表示。実データで 8136・3166 が発注可能になることを確認 |
| **1.13.1.a** | 2026-09-03 | 軽微 | **買い候補が全滅していた設定と、それを隠していたログの修正**：①`high_risk.max_position_ratio` を 0.28→0.50（余力48万円時に1銘柄上限が13.5万円しかなく、単元価格の中央値17.6万円にすら届かず**買えるのは43銘柄中14銘柄だけ**だった。実際 9/3 の朝発注は候補8銘柄すべてが上限超過で見送り。0.50 で25銘柄まで拡大）②見送り理由のログを「余力不足」から**判定に使った数字入りの文面**へ（`単元(100株)の必要額 590,900円 が1銘柄上限 242,002円 を超過（株価5,909円・上限比率50%・実効余力484,005円）`。現金不足と誤読され原因究明を妨げていた）③`switch-mode.bat` がアプリ停止後も窓を残していた問題を修正（終了コードで判定し、正常終了時のみ閉じる。起動拒否時はメッセージを読めるよう残す） |
| **1.13.1** | 2026-09-02 | 小規模⑩ | **監視3層の穴を塞ぐ**（1.13.0 で実装した監視をすり抜けて同じ事故が再発した。8:30のトークン更新成功後にセッションが切れ、401が497回・9:05の売り注文が失敗・損切り監視も終日停止したが**通知は1件も出なかった**）：①場中の401を認証切れとして記録（REST呼び出しを共通経路に集約。従来は起動時と8:30の2か所しか見ておらず、**自動復帰・reconnect・通知が同時に全滅**していた）②liveness の「一度も成功していない」を**無期限グレースにしない**（引け後に起動したプロセスは監視が永久に無効だった）③エラー率を**レベル別閾値**に（401の大半はWARNINGでERRORは28件しかなく閾値10に届かなかった）④通知送信の**再送**（DNS障害でハートビートが消えた）⑤`.env.*` を一括gitignore（`.env.bak` にAPIパスワード平文が未追跡で残存。履歴混入は無し）⑥`KABU_DASHBOARD_TOKEN` の固定値設定 |
| **1.13.0** | 2026-09-01 | メジャー | live運用で判明した課題への対応（実際に発生した事故が起点）：①ログイン認証切れの検知・通知・**時間制限なしの自動復帰**（2営業日気づけず稼働していた事故／30分で諦める欠陥の修正）②**監視3層**（エラー率・ハートビート・サイレント故障）③**東証休場日対応**（祝日・年末年始。jpholiday導入）④**Discordリモコン**（ポート開放不要のポーリング方式。状態確認・停止・再接続・kabuステーション起動）⑤約定通知と重要度マーク（🔴🟡🟢⚪）⑥日次レポートに含み損益・評価額を追加／週次サマリ⑦取引頻度向上（8銘柄×28%・後場スロット12:35・早めの利確）⑧トレーリングストップのKeyError修正（**利確が毎回失敗していた**） |
| **1.12.9.a** | 2026-06-24 | 軽微 | ウォッチリスト大型リストに17銘柄追加（3387・6753・9302・8136・2353・3289・4540・2229・3563・3660・6306・2157・3034・8934・8923・8804・8850） |
| 1.12.9.0 | 2026-06-24 | 中規模⑫ | トレーリングストップ＋ブレークイーブンによる利益確定機能：保有中の最高値(`peak_price`)を5分ごとに追跡し、ピーク含み益が`breakeven_trigger_pct`(+3%)に達したら損切りラインを`avg_cost`（ブレークイーブン）まで引き上げ、以後は`peak_price×(1-trailing_stop_pct=5%)`のトレーリングラインで利益確定。`positions`テーブルに`peak_price`列を追加（既存DBに自動付与）。両パラメータが0なら従来の損切りのみと完全に一致。テスト36件追加（全618件パス） |
| 1.11.9.0 | 2026-06-23 | 中規模⑪ | 日次損益レポートのX/Discord自動投稿機能：平日17:30に当日/週次/月次/総合の損益（円・%）と勝率を自動投稿。`pnl_report.py`（損益集計）・`x_poster.py`（X OAuth 1.0a・280文字）・`discord_report.py`（Discord Webhook・2000文字）・`reference_capital.py`（live/dry_run/semi_liveの基準資金GUI設定）を追加。X/Discordは完全に独立し、どちらか一方のみまたは両方を有効化可能。ダッシュボードに設定カード・投稿テキスト確認ボタンを追加 |
| 1.10.9.0 | 2026-06-21 | 小規模⑨ | LINE Notify廃止しDiscord Webhookへ移行（LINE Notifyは2025年3月31日にサービス終了済みで実際には通知不達だった）。`AlertProvider`プロトコル抽象＋`DiscordWebhookProvider`を実装し、将来Slack/Telegram等を追加する際も`alert()`呼び出し側（17箇所）は無改修で済む設計に変更。2000文字超は安全に切り詰め、1つのプロバイダの失敗は他をブロックしない |
| 1.10.8.0 | 2026-06-21 | 小規模⑧ | 外部レビュー対応(P0/P1/P2)：含み損を当日合計ドローダウンに統合しkill switch自動作動・MLモデルのSHA256改ざん検知（fail-closed、メタ無しTOFUバイパスを閉鎖）・ダッシュボードセキュリティ強化（?token=のクリーンURLリダイレクト・Cookie Secure・ログインレート制限）・流動性ガード（平均売買代金下限・大引け間際BUY抑止）・`BrokerGateway`/`broker_constants`分離・バックテストにスリッページ/手数料/profit factor/Sortino追加・semi_live `APPROVING`状態・相関IDログ・運用Runbook追加 |
| **1.10.7.b** | 2026-06-20 | 軽微 | フォローレビュー対応：プロセスロック解放を`atexit`に一元化し起動途中の`sys.exit`/例外でもロックを残さない、dry_runが本番口座を読む際の警告を起動バナーにもWARNINGで明示、paper多重起動を`runtime.allow_multiple_paper_instances`(既定false=中断)で制御、設計書に「意図的に見送った決定事項(Deferred Decisions)」を明記 |
| 1.10.7.a | 2026-06-20 | 小規模⑦ | 厳格レビュー対応（P0/P1）：緊急全決済をローカルDBではなくブローカー`/positions`基準に変更、退出注文前の競合キャンセル失敗時に成行売り送信をブロック、ブローカー側で確定終了した部分約定を専用状態(`PARTIALLY_FILLED_DONE`)に分離（旧実装は未約定注文として永久に新規発注をブロックし続けるバグがあった）、`/positions`によるDBとの建玉ドリフト検知＋検知時のkill switch自動作動、多重起動防止のプロセスロック、dry_runが本番口座データを読む際のプリフライト警告 |
| 1.10.6.0 | 2026-06-20 | 中規模⑩ | 注文モデル分離 OrderIntent/BrokerOrder/Fill（Phase5最終項目・4.2）：意図・ブローカー注文・約定明細の3層分離、平均単価会計→FIFOロット会計へ移行、schema_version v2で既存データをバックフィルし過去の実現損益も遡及再計算。これでPhase1〜5の全項目が完了 |
| 1.9.6.0 | 2026-06-20 | 中規模⑨ | Phase5（OrderIntent分離の手前まで）：パフォーマンス分析・異常検知アラート・取引根拠記録・バックテスト実績乖離分析・ブローカー側逆指値ストップ（保護）・RiskSnapshot（リスク判定の一括スナップショット） |
| 1.8.6.0 | 2026-06-20 | 小規模⑥ | Phase5（低リスク分）：依存のバージョン固定(再現性)・schema_versionによるマイグレーション版数管理・日次レポート/取引ジャーナル(メモ記録)API＋ダッシュボード |
| 1.8.5.0 | 2026-06-20 | 中規模⑧ | 運用安全機能＋機能要望（Phase3/4）：取引停止スイッチ(kill switch)・dry_run/semi_liveモード（発注承認キュー）・ライブ起動前プリフライトチェック・運用状態の可視化、ウォッチリスト重複防止(409)・バックテスト履歴管理(ページング/削除/アーカイブ)・カスタムリスクプロファイル(検証/clone/import-export/compare/履歴) |
| 1.7.5.c | 2026-06-20 | 軽微 | ダッシュボード既定hostを `0.0.0.0` 前提にドキュメント統一・バージョン採番ルール／修正履歴を追加 |
| 1.7.5.b | 2026-06-20 | 軽微 | ドキュメントとコードの整合修正（スケジュール表・売買閾値・ウォッチリスト名・リスク値） |
| 1.7.5.a | 2026-06-20 | 軽微 | CIがUTCランナーで常に失敗していたタイムゾーンテストの前提ミスを修正 |
| 1.7.5.0 | 2026-06-19〜20 | 中規模⑦ | 安全性大改修：緊急決済・損切りのリスクゲート貫通修正、約定状態の定期照合(reconcile_orders)、未約定BUYの引当、fail-closed起動 |
| 1.6.5.a | 2026-06-19 | 軽微 | CI依存インストール修正・外部レビュー6件対応・LAN公開host/URL修正・ユーザーIDハッシュ化 |
| 1.6.5.0 | 2026-06-18〜19 | 小規模⑤ | 販売向けセキュリティ：ログイン認証(PBKDF2)・ログのlog/フォルダ分離・日付/レベル別出力 |
| 1.6.4.a | 2026-06-18 | 軽微 | 50万円投入時のライブ取引挙動を説明するドキュメント追加 |
| 1.6.4.0 | 2026-06-18 | 中規模⑥ | 複数ウォッチリスト管理・バックテスト閾値オーバーライド |
| 1.5.4.0 | 2026-06-17 | 中規模⑤ | リスクプロファイル切替(low_risk/high_risk)・タイムゾーン整合・リスク管理の結線修正 |
| 1.4.4.0 | 2026-06-17 | 小規模④ | LAN公開対応・モバイル向けレスポンシブ表示 |
| 1.4.3.0 | 2026-06-17 | 小規模③ | ウォッチリスト追加時の過去データ自動取得・会社名自動入力・全角コード正規化 |
| 1.4.2.0 | 2026-06-16 | 小規模② | データ更新API(`/api/data/update`)・銘柄欄への会社名表示 |
| 1.4.1.a | 2026-06-16 | 軽微 | pandas純正実装化(pandas-ta除去)・yfinance MultiIndex対応・閾値0.6→0.25・CI修正 |
| 1.4.1.0 | 2026-06-16 | 中規模④ | ライブモードSELLシグナル自動執行 |
| 1.3.1.0 | 2026-06-16 | 中規模③ | 損益可視化強化（日次棒グラフ・含み損益・MTD/YTD・シャープ/最大DD） |
| 1.2.1.0 | 2026-06-16 | 中規模② | MLモデル精度トラッキング |
| 1.1.1.0 | 2026-06-16 | 中規模① | バックテスト機能（ウォークフォワード方式） |
| 1.0.1.0 | 2026-06-16 | 小規模① | ラベリングをトリプルバリア法（López de Prado）へ強化 |
| 1.0.0.0 | 2026-06-16 | メインリリース | 初期実装：自動売買・リスク管理・Webダッシュボード・概要/詳細設計書 |

---

## 1. システム概要

### 1.1 目的

日本株（東京証券取引所）を対象としたスイングトレード（数日〜数週間保有）の完全自動売買システム。auカブコム証券のkabuステーションAPIを使用し、ルールベース＋機械学習ハイブリッド戦略でシグナルを生成・発注する。

### 1.2 対象ユーザー

個人投資家（システム管理者兼利用者）

### 1.3 運用形態

| 項目 | 内容 |
|------|------|
| 稼働形態 | 24時間365日常駐起動 |
| 取引時間 | 東証前場 9:00〜11:30 / 後場 12:30〜15:30（平日のみ） |
| 取引モード | paper（ペーパートレード）/ live（本番） |
| 運用資金 | 50万円以下（小規模） |

---

## 2. システム構成

### 2.1 構成図

```
┌─────────────────────────────────────────────────────────┐
│                   ノートPC（Windows）                      │
│                                                         │
│  ┌──────────────────┐    ┌───────────────────────────┐  │
│  │  kabuステーション  │    │       kabu-auto           │  │
│  │  （公式アプリ）    │◄──►│                           │  │
│  │  localhost:18080  │    │  ┌─────────────────────┐  │  │
│  └──────────────────┘    │  │   スケジューラ        │  │  │
│                           │  │   (APScheduler)      │  │  │
│  ┌──────────────────┐    │  └──────────┬──────────┘  │  │
│  │   yfinance       │    │             │               │  │
│  │（過去データ取得） │◄──►│  ┌──────────▼──────────┐  │  │
│  └──────────────────┘    │  │  戦略エンジン         │  │  │
│                           │  │  ルール + ML         │  │  │
│  ┌──────────────────┐    │  └──────────┬──────────┘  │  │
│  │  SQLite DB       │◄──►│             │               │  │
│  │  kabu_auto.db    │    │  ┌──────────▼──────────┐  │  │
│  └──────────────────┘    │  │  発注・ポジション管理  │  │  │
│                           │  └─────────────────────┘  │  │
│  ┌──────────────────┐    │                             │  │
│  │  Webダッシュボード│◄──►│  FastAPI (port:8080)       │  │
│  │  ブラウザで確認  │    └───────────────────────────┘  │
│  └──────────────────┘                                   │
└─────────────────────────────────────────────────────────┘
                              │
          ┌──────────────┴──────────────┐
┌─────────▼─────────┐   ┌───────────▼──────────┐
│  Discord Webhook  │   │  X（旧Twitter）        │
│  （異常時アラート）│   │  （日次レポート・任意）│
│  （日次レポート） │   └──────────────────────┘
└───────────────────┘
```

### 2.2 ディレクトリ構成

```
kabu-auto/
├── main.py                  # エントリポイント
├── config.yaml              # 設定ファイル（機密情報・銘柄リストは含まない）
├── watchlists.json          # ウォッチリスト（複数リスト対応・銘柄コード・会社名・セクター、GUIから編集可）
├── risk_profile.json        # アクティブなリスクプロファイル（GUIから切替・git管理外）
├── .env                     # 機密情報（kabuステーションのAPIパスワード等、git管理外）
├── .env.example             # .env のテンプレート
├── requirements.txt         # 依存パッケージ
├── src/
│   ├── api/                 # kabuステーションAPIクライアント
│   ├── core/                # 設定・ウォッチリスト・ログ・スケジューラ・アラート
│   ├── data/                # DB管理・市場データ取得
│   ├── strategy/            # テクニカル指標・MLモデル・シグナル生成
│   ├── execution/           # 発注・ポジション管理
│   ├── risk/                # リスク管理
│   └── dashboard/           # Webダッシュボード（FastAPI）
├── frontend/                # ダッシュボードUI（HTML/JS）
├── models/                  # 学習済みMLモデル（pickle）
├── data/                    # SQLite DB・ログ・バックアップ
│   └── backups/
├── scripts/                 # 運用スクリプト（モード切替・状態バックアップ）
└── docs/                    # 設計書
```

> **設定ファイルの分離方針**: `config.yaml` はgit管理され複数環境で共有される静的設定、
> `watchlists.json` はダッシュボードGUIから動的に追加・削除される銘柄リスト（複数リスト対応）、
> `risk_profile.json` はGUIから切り替えるアクティブなリスクプロファイル（個人用・git管理外）、
> `.env` は `.gitignore` 済みで各環境のみに存在する機密情報（APIパスワード等）。
> これらを分離することで、`git pull` 時に個人設定（パスワード・ウォッチリスト・リスク設定）が
> リポジトリ側の更新と競合する問題を避けられる。

---

## 3. 機能一覧

### 3.1 主要機能

| No. | 機能 | 説明 |
|-----|------|------|
| F01 | 市場データ取得 | yfinanceで権利修正済み日足OHLCVを取得・DB保存 |
| F02 | テクニカル指標計算 | MA・RSI・ボリンジャーバンド・MACDを計算 |
| F03 | MLモデル学習 | LightGBMで5日後上昇を予測するモデルを学習。ロード時にSHA256整合性検証・改ざん検知（fail-closed） |
| F04 | シグナル生成 | ルールベース＋MLスコアを統合して売買判断 |
| F05 | 発注実行 | kabuステーションAPIで発注。通常エントリ/エグジットは指値、損切り・緊急決済は成行（二重発注防止・約定冪等処理・15秒間隔の状態照合） |
| F06 | ポジション管理 | 保有銘柄・平均取得単価・損益をDB管理 |
| F07 | リスク管理 | 損切り・ブレークイーブン・トレーリングストップ・流動性ガード（平均売買代金・大引け前BUY抑止）・最大銘柄数・セクター集中・注文上限・当日損失上限（含み損含む合計ドローダウン）を制御 |
| F08 | Webダッシュボード | ポジション・損益・シグナル履歴をブラウザで確認 |
| F09 | 緊急全決済 | ダッシュボードから全ポジションを即時決済 |
| F10 | アラート通知 | 損切り実行・API異常時に通知（Discord Webhook。将来複数プロバイダ対応可能） |
| F11 | 自動バックアップ | SQLite DBを毎日バックアップ（30日分保持）。稼働中でも整合するようオンラインバックアップAPIで取得 |
| F12 | 週次再学習 | 毎週日曜2:00にMLモデルを自動再学習 |
| F13 | 朝の発注実行 | 前日シグナルを元に9:05にSELL優先・BUYの順で自動発注（ライブモード） |
| F14 | CI自動テスト | GitHub Actions でプッシュ毎に全ユニットテストを自動実行 |
| F15 | データ手動更新API | `/api/data/update` で過去データ取得を即時実行（初回セットアップ用） |
| F16 | ウォッチリストGUI管理 | ダッシュボードから銘柄コード・会社名・セクターを追加・削除。複数リストの作成・切替・改名・JSON出力/取込に対応（`watchlists.json`、再起動不要） |
| F17 | 機密情報の分離管理 | kabuステーションのAPIパスワードを `.env`（環境変数）で管理し `config.yaml` に平文保存しない |
| F18 | リスクプロファイル切替 | ローリスク⇔ハイリスクの2プロファイルをGUIからワンクリック切替（`risk_profile.json`、再起動不要） |
| F19 | 注文状態の定期照合 | 15秒間隔でブローカーの未約定注文とDBを照合し、WebSocket取り逃し・再起動を跨いだ状態ズレを補正 |
| F20 | X/Discord日次レポート自動投稿 | 平日17:30に当日/週次/月次/総合の損益（円・%）と勝率をX（OAuth 1.0a）またはDiscord Webhookへ自動投稿（各々独立・任意設定。未設定時はスキップのみ） |
| F21 | ログイン認証の自動復帰 | kabuステーションの認証は毎営業日切れる。検知→通知→**時間制限なく**再接続を試み、ログインされ次第そのまま復帰する。復帰が場中かつ締切前なら逃した発注も取り返す |
| F22 | 監視3層（機能しているかの監視） | ①エラー率（時間窓のERROR多発）②ハートビート（平日8:45に生存通知）③サイレント故障（場中のデータ途絶）。従来の「取引の結果」の監視では捉えられない障害を検知する |
| F23 | 東証休場日対応 | 土日・国民の祝日（jpholiday）・年末年始(12/31-1/3)を休場と判定。休場日は無意味なシグナル生成・通知を止める |
| F24 | Discordリモコン | **ポート開放不要**（30秒ポーリング）。status / positions / today / pnl / orders / reconnect / launch / halt / resume / help。秘密情報の授受と全決済は意図的に非実装 |
| F25 | kabuステーションの起動制御 | アプリが落ちていれば自動/手動で起動（1日3回まで）。**認証は自動化しない**（承認は認証アプリで人が行う） |
| F26 | 後場の発注スロット | 12:35に、朝に見送った**未保有**銘柄のみを拾い直す |
| F27 | 通知の重要度 | 🔴要対応 / 🟡注意 / 🟢実行報告 / ⚪定期。約定（買い・売り）も通知する |
| F28 | 状態バックアップ | DB・モデル・設定・運用状態・機密をZIP化し**PCの外**（OneDrive等）へ退避。コードはGitHub、状態はZIPの2点で端末故障から復元できる（`scripts/backup_state.ps1`） |

### 3.2 スケジュール機能

| 時刻 | 処理 | 条件 |
|------|------|------|
| 平日 8:25 | 日次リスクカウンタ（注文数・損失額）のリセット | 平日のみ |
| 毎朝 8:30 | APIトークン更新 | 毎日 |
| 9:05 | 前日シグナルを元に発注（SELL優先→BUY） | 平日・ライブモードのみ |
| 9:00〜11:30 / 12:30〜15:30 (5分毎) | 損切りチェック | 平日のみ |
| 16:00 | 市場データ更新 | 毎日 |
| 16:20 | シグナルスキャン（翌営業日の売買候補を生成、データ更新の後） | 平日のみ |
| 17:00 | DBバックアップ | 毎日 |
| 平日 17:30 | X/Discord日次レポート自動投稿（当日/週次/月次/総合の損益・勝率） | 平日・有効化設定時のみ（任意） |
| 毎週日曜 2:00 | MLモデル再学習 | 週次 |
| 15秒間隔 | 未約定注文の状態照合（reconcile_orders） | 市場時間中のみ実働 |
| 平日 8:45 | ハートビート（稼働中です）通知 | 平日・休場日を除く |
| 12:35 | 後場の発注スロット（未保有銘柄のみ） | 平日・ライブモード系 |
| 土曜 9:00 | 週次サマリをDiscordへ投稿 | 週次 |
| 5分間隔 | 認証切れからの復帰試行（切れている間だけ） | 常時 |
| 30秒間隔 | Discordリモコンのコマンド受信 | 設定時のみ |
| 15分間隔 | 異常検知（エラー率・サイレント故障を含む） | 平日8:00-23:00 |

---

## 4. 外部インターフェース

### 4.1 kabuステーションAPI

| 項目 | 内容 |
|------|------|
| 種別 | REST API / WebSocket |
| エンドポイント | http://localhost:18080/kabusapi |
| 認証 | APIトークン（X-API-KEYヘッダー） |
| データ形式 | JSON |
| 主な用途 | トークン取得・板情報・発注・ポジション照会・約定受信 |

### 4.2 yfinance

| 項目 | 内容 |
|------|------|
| 種別 | Pythonライブラリ（Yahoo Finance API） |
| 用途 | 権利修正済み過去OHLCV取得 |
| 銘柄コード形式 | {証券コード}.T（例: 7203.T） |

> yfinance 1.x の `download()` は MultiIndex カラム（例: `('Close', '7203.T')`）を返すため、
> `src/data/market_data.py` で `get_level_values(0)` によりフラット化してから処理する。

### 4.3 アラート通知（Discord Webhook）

| 項目 | 内容 |
|------|------|
| 種別 | REST API（Webhook） |
| 用途 | 異常時アラート通知 |
| 設定 | config.yaml の alerts.discord_webhook_url |
| 任意 | 未設定の場合は通知なし |
| 備考 | プロバイダ抽象化されており、将来Slack/Telegram/email等を追加可能（詳細: docs/通知設定ガイド.md） |

### 4.4 Webダッシュボード

| 項目 | 内容 |
|------|------|
| URL | http://localhost:8080 |
| 技術 | FastAPI + Chart.js |
| 更新頻度 | 10秒ごと自動更新 |

---

## 5. データ設計（概要）

### 5.1 データベース

SQLite（WALモード有効）。ファイルパス: `data/kabu_auto.db`

| テーブル | 説明 |
|---------|------|
| ohlcv | 日足OHLCV（権利修正済み） |
| positions | 現在の保有ポジション |
| trades | 注文・約定履歴 |
| signals | シグナル生成履歴 |

### 5.2 モデルファイル

学習済みLightGBMモデルをpickle形式で保存。パス: `models/lgb_model.pkl`

---

## 6. 取引戦略概要

### 6.1 ハイブリッドシグナル

```
統合スコア = ルールベーススコア × 0.5 + MLスコア × 0.5  （理論レンジ -1.0 〜 +1.0）

統合スコア >= +0.25 → BUY シグナル
統合スコア <= -0.25 → SELL シグナル
それ以外            → HOLD
```

> **閾値の根拠**: 上の ±0.25 は `config.yaml` の `strategy` セクションの基準値だが、
> 実運用ではアクティブなリスクプロファイル（既定 `low_risk`: ±0.14 / `high_risk`: ±0.08）の
> 値で上書きされる。再調整の経緯は[詳細設計書](詳細設計書.md)を参照。
> ルール／MLスコアを重み付き平均するため統合スコアの実効レンジは狭く、実測スコア分布
> （2026-06時点: 平均0.043・標準偏差0.043）に合わせて閾値を引き下げている。
> 理論上限 1.0 を前提とした旧値は実効レンジ外で発火せず、要調整パラメータである。

### 6.2 使用テクニカル指標

| 指標 | パラメータ | 用途 |
|------|-----------|------|
| 移動平均線 | 短期5 / 中期25 / 長期75 | MAクロスでトレンド判定 |
| RSI | 14日 | 売られすぎ(30以下)/買われすぎ(70以上) |
| ボリンジャーバンド | 20日 / 2σ | バンド外れで反転シグナル |
| MACD | 標準設定 | ヒストグラム転換でシグナル |

> 全指標は `pandas` のみで実装（`pandas-ta` 非依存）。
> `pandas-ta==0.3.14b0` が Python 3.11 向けに PyPI で配布されていないため、
> SMA/RSI/BBands/MACD を `src/strategy/indicators.py` 内で直接計算する。

### 6.3 機械学習モデル

| 項目 | 内容 |
|------|------|
| アルゴリズム | LightGBM（二値分類） |
| 予測対象 | 損切り前に利益目標へ到達するか（トリプルバリア法 1/0） |
| 特徴量 | テクニカル指標10種 |
| ラベリング | トリプルバリア法（López de Prado）＋サンプル一意性重み |
| 学習方式 | 時系列交差検証（5分割）→全データで最終学習 |
| 再学習 | 毎週日曜2:00 |

> 2025-2026年の比較研究では、日足スイングのようなサンプル数が限られる表形式
> 金融データでは勾配ブースティング（LightGBM）がTransformer系深層学習より
> 優位とされる。最新トレンドを踏まえモデルはLightGBMを維持し、ラベリングを
> 最新ベストプラクティスのトリプルバリア法へ強化している。

---

## 7. リスク管理概要

> 数値はアクティブなリスクプロファイルで決まる（既定 `low_risk`。下表は代表値）。
> プロファイル別の正確な値は[詳細設計書](詳細設計書.md)・`config.yaml` を参照。

| リスク | 対策 |
|--------|------|
| 過大投資 | 1銘柄あたり最大投資比率を制限（low_risk:12% / high_risk:35%） |
| 急落 | 損切りライン（low_risk:-4% / high_risk:-10%）で強制成行売却 |
| 過集中 | 同一セクター集中上限（low_risk:30% / high_risk:55%） |
| 流動性不足 | 指値注文＋5分タイムアウトキャンセル |
| 過剰発注 | 1日の注文数上限100件 |
| 連続損失 | 当日損失上限（low_risk:¥15,000 / high_risk:¥60,000）を超えたら発注停止（再起動後もDBから復元） |
| 二重発注 | DB未約定チェック＋15秒間隔の状態照合により同銘柄への重複発注を防止 |
| ギャップ | 最大同時保有銘柄数を制限（low_risk:6 / high_risk:4）してリスク分散 |
| DB損失 | 日次バックアップ（30日分。オンラインバックアップAPIでWAL未反映分も含む） |
| 端末故障 | 状態バックアップZIPをPCの外（OneDrive等）へ退避。コードはGitHub、状態はZIPの2点で復元（`scripts/backup_state.ps1`） |
| システム異常 | ダッシュボードから緊急全決済ボタン（リスクゲートをバイパスして確実に決済） |
| 誤起動 | ライブモード起動時にCONFIRM_LIVE_TRADING=true 環境変数を必須化 |
| 状態誤認 | ライブ起動時のトークン取得失敗・ポート競合は起動中断（fail-closed） |

---

## 8. 非機能要件

| 項目 | 要件 |
|------|------|
| 可用性 | 24/365稼働（PC起動中） |
| パフォーマンス | シグナル生成 < 1秒/銘柄 |
| セキュリティ | APIパスワードは環境変数（`.env`）で管理（`config.yaml` には平文保存しない） |
| 拡張性 | ウォッチリスト銘柄は `watchlists.json`／GUIで変更可能（複数リスト対応） |
| 保守性 | loguru によるローテーションログ（既定15日分保持・INFO以下/WARNING以上で別ファイル） |

---

## 9. 制約・前提条件

- kabuステーション（Windowsアプリ）が常時起動していること
- PCのスリープを無効化していること（スリープでAPI切断）
- kabuステーションのAPIトークンは毎朝8:30に自動更新
- WebSocketは切断時に自動再接続（最大60秒間隔）
- 土曜1〜5時はkabuシステムメンテナンスのため処理を停止
- **ライブモード起動時**は環境変数 `CONFIRM_LIVE_TRADING=true` が必須（誤起動防止）
- 起動時にAPI上の未約定注文とDBを同期する。API側で見つかった注文は実Stateへ収束させ、APIに見つからない未解決注文は約定済みの誤取消を避けるため UNKNOWN とし、人手確認まで発注を抑止する（CANCELLEDにはしない）
