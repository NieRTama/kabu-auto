# kabu-auto 詳細設計書

| 項目 | 内容 |
|------|------|
| システム名 | kabu-auto |
| バージョン | 1.0.0 |
| 作成日 | 2026-06-16 |

---

## 1. モジュール詳細

### 1.1 src/core/config.py — 設定管理

**責務**: `config.yaml` の読み込みとアプリ全体への提供

| 関数 | 引数 | 戻り値 | 説明 |
|------|------|--------|------|
| `load(path)` | path: str | dict | YAMLを読み込みキャッシュ。読み込み後に `_apply_env_overrides()` を実行 |
| `get()` | なし | dict | キャッシュ済み設定を返す |
| `get_section(section)` | section: str | dict | セクション単位で設定を返す |
| `_apply_env_overrides(config)` | config: dict | なし | 環境変数 `KABU_API_PASSWORD` が設定されていれば `kabu_station.password` を上書き |

> **機密情報の取り扱い**: `config.yaml` はgit管理されるため、kabuステーションのパスワードを
> 平文で書かない運用とする。`main.py` 起動時に `python-dotenv` で `.env`（`.gitignore`済み）を
> 読み込み、環境変数 `KABU_API_PASSWORD` を `config.yaml` の値に優先して適用する。
> `.env` が存在しない／変数未設定の場合は `config.yaml` の `password` 値（通常は空文字）を使う。

---

### 1.1.1 src/core/watchlist.py — ウォッチリスト管理

**責務**: 監視・取引対象銘柄（コード・会社名）を `watchlist.json` で永続化し、
ダッシュボードGUIからの追加・削除をプロセス再起動なしで反映する。

| 関数 | 引数 | 戻り値 | 説明 |
|------|------|--------|------|
| `load(path)` | path: str | list[dict] | JSONを読み込みキャッシュ。ファイルが無ければ空リスト |
| `get_all()` | なし | list[dict] | `[{"code": ..., "name": ...}, ...]` を返す |
| `get_codes()` | なし | list[str] | 銘柄コードのみのリストを返す |
| `get_names()` | なし | dict[str, str] | コード→会社名マッピングを返す |
| `get_sectors()` | なし | dict[str, str] | コード→セクターマッピングを返す（セクター集中チェック用） |
| `add(code, name)` | code, name: str | list[dict] | 追加（既存コードなら会社名を更新）し即座にファイル書き込み |
| `update_sector(code, sector)` | code, sector: str | None | セクターを更新（空文字なら何もしない） |
| `remove(code)` | code: str | list[dict] | 削除し即座にファイル書き込み |

`main.py` の各スケジュールジョブ（データ更新・損切りチェック・シグナルスキャン等）は
ループ開始時に `watchlist_store.get_codes()` を呼ぶため、GUIでの変更は次回のジョブ実行
（最短で5分後の損切りチェック）から反映される。

---

### 1.2 src/core/scheduler.py — スケジューラ

**責務**: 定時処理の登録・実行管理（Asia/Tokyo固定）

**クラス: `TradingScheduler`**

| メソッド | 説明 |
|---------|------|
| `register(name, callback)` | コールバック関数を名前で登録 |
| `start()` | 全ジョブをAPSchedulerに登録して起動 |
| `stop()` | スケジューラを停止 |
| `is_market_open()` | 現在が東証取引時間内かチェック（静的） |
| `is_maintenance_window()` | 土曜深夜メンテ時間帯かチェック（静的） |

**取引時間判定ロジック**:
```
平日（月〜金）かつ
  前場: 9:00 <= 現在時刻 < 11:30
  または
  後場: 12:30 <= 現在時刻 <= 15:30
```

**登録ジョブ一覧**（すべて Asia/Tokyo）:

| ジョブID | 時刻 | 内容 |
|---------|------|------|
| `risk_reset` | 平日 8:25 | `RiskManager.reset_daily_counters()`（注文数・損失額リセット） |
| `token_refresh` | 毎日 8:30 | kabuステーションAPIトークン更新 |
| `morning_execution` | 平日 9:05 | 前日シグナルに基づく発注（ライブモードのみ） |
| `stop_loss_check` | 平日 9-15時 5分おき | 損切りライン到達チェック |
| `data_update` | 毎日 16:00 | yfinanceで当日分OHLCVを取得・DB反映 |
| `signal_scan` | 平日 16:20 | 翌営業日の売買候補スキャン（`data_update` より後ろの時刻に固定） |
| `db_backup` | 毎日 17:00 | DBバックアップ |
| `ml_retrain` | 日曜 2:00 | MLモデル週次再学習 |

---

### 1.3 src/api/kabu_client.py — APIクライアント

**責務**: kabuステーションAPI（REST/WebSocket）の操作

**クラス: `KabuClient`**

#### REST APIメソッド

| メソッド | エンドポイント | 説明 |
|---------|--------------|------|
| `refresh_token()` | POST /token | APIトークン取得・更新 |
| `get_board(symbol, exchange)` | GET /board/{symbol}@{exchange} | 板情報・現在値取得 |
| `get_symbol(symbol, exchange)` | GET /symbol/{symbol}@{exchange} | 銘柄情報取得 |
| `get_positions()` | GET /positions | 保有ポジション一覧 |
| `get_orders(query)` | GET /orders | 注文一覧 |
| `get_wallet()` | GET /wallet/cash | 現金残高（余力） |
| `send_order(order)` | POST /sendorder | 注文発注 |
| `cancel_order(order_id)` | PUT /cancelorder | 注文キャンセル |
| `register_push(symbols)` | PUT /register | WebSocketへの銘柄登録 |
| `unregister_all()` | PUT /unregister/all | 全銘柄の登録解除 |

#### WebSocket

| メソッド | 説明 |
|---------|------|
| `start_websocket(on_price, on_order_event)` | WebSocket開始（別スレッド） |
| `stop_websocket()` | WebSocket停止 |

**WebSocket再接続ロジック**:
```
接続失敗時: 2秒待機 → 4秒 → 8秒 → ... → 最大60秒
切断時: is_reconnect フラグが True の間は自動再接続
```

**受信イベントの振り分け**:
- `"Symbol"` キーがあれば → `on_price` コールバック（価格更新）
- `"OrderEvent"` キーがあれば → `on_order_event` コールバック（約定通知）

---

### 1.4 src/data/database.py — データベース

**責務**: SQLiteの初期化・テーブル定義・バックアップ

**接続設定**:
- WALモード有効（複数スレッドからの同時アクセス対応）
- `check_same_thread=False`（マルチスレッド許可）

**`get_session()` — セッション取得（`@contextmanager`）**:
- `yield` でセッションを提供し、例外発生時は明示的に `rollback()`、`finally` で必ず `close()` を実行
- すべての呼び出し元は `with get_session() as session:` パターンで使用

#### テーブル定義

**ohlcv テーブル**

| カラム | 型 | 制約 | 説明 |
|-------|-----|------|------|
| id | Integer | PK | 自動採番 |
| symbol | String(10) | NOT NULL | 銘柄コード |
| date | Date | NOT NULL | 日付 |
| open | Float | | 始値 |
| high | Float | | 高値 |
| low | Float | | 安値 |
| close | Float | | 終値 |
| volume | Integer | | 出来高 |
| adjusted_close | Float | | 権利修正済み終値 |

インデックス: `(symbol, date)` UNIQUE

**positions テーブル**

| カラム | 型 | 制約 | 説明 |
|-------|-----|------|------|
| id | Integer | PK | 自動採番 |
| symbol | String(10) | UNIQUE | 銘柄コード |
| quantity | Integer | NOT NULL | 保有株数 |
| avg_cost | Float | NOT NULL | 平均取得単価 |
| sector | String(50) | | セクター名 |
| opened_at | DateTime | | 最初の取得日時 |
| updated_at | DateTime | | 最終更新日時 |

**trades テーブル**

| カラム | 型 | 制約 | 説明 |
|-------|-----|------|------|
| id | Integer | PK | 自動採番 |
| order_id | String(50) | UNIQUE | 注文ID |
| symbol | String(10) | NOT NULL | 銘柄コード |
| side | String(4) | NOT NULL | BUY / SELL |
| quantity | Integer | NOT NULL | 数量 |
| price | Float | | 注文価格 |
| filled_at | DateTime | | 約定日時 |
| status | String(20) | | PENDING / FILLED / CANCELLED |
| pnl | Float | | 損益（売り時に記録） |
| note | Text | | メモ |

**signals テーブル**

| カラム | 型 | 制約 | 説明 |
|-------|-----|------|------|
| id | Integer | PK | 自動採番 |
| symbol | String(10) | NOT NULL | 銘柄コード |
| generated_at | DateTime | | 生成日時 |
| rule_score | Float | | ルールベーススコア |
| ml_score | Float | | MLスコア |
| combined_score | Float | | 統合スコア |
| action | String(10) | | BUY / SELL / HOLD |

**model_metrics テーブル**

| カラム | 型 | 制約 | 説明 |
|-------|-----|------|------|
| id | Integer | PK | 自動採番 |
| trained_at | DateTime | | 学習日時 |
| cv_mean_accuracy | Float | | CVスコア平均 |
| cv_std_accuracy | Float | | CVスコア標準偏差 |
| n_samples | Integer | | 学習サンプル数 |
| n_estimators | Integer | | 最終estimators数 |
| feature_importances_json | Text | | 特徴量重要度（JSON） |
| trigger | String(20) | default="manual" | "weekly_schedule" / "manual" |

**backtest_runs テーブル**

| カラム | 型 | 制約 | 説明 |
|-------|-----|------|------|
| id | Integer | PK | 自動採番 |
| symbol | String(10) | NOT NULL | 銘柄コード |
| start_date | Date | NOT NULL | 開始日 |
| end_date | Date | NOT NULL | 終了日 |
| initial_capital | Float | | 初期資金 |
| final_capital | Float | | 最終資産 |
| total_return | Float | | トータルリターン（率） |
| max_drawdown | Float | | 最大ドローダウン（率） |
| sharpe_ratio | Float | | シャープレシオ（年率） |
| win_rate | Float | | 勝率 |
| trade_count | Integer | | 取引数 |
| use_ml | Integer | default=0 | ML使用フラグ |
| created_at | DateTime | | 実行日時 |
| equity_curve_json | Text | | エクイティカーブ（JSON） |

**backtest_trades テーブル**

| カラム | 型 | 制約 | 説明 |
|-------|-----|------|------|
| id | Integer | PK | 自動採番 |
| run_id | Integer | NOT NULL | backtest_runs.id への外部参照 |
| symbol | String(10) | | 銘柄コード |
| entry_date | Date | | エントリー日 |
| entry_price | Float | | エントリー価格 |
| exit_date | Date | | エグジット日 |
| exit_price | Float | | エグジット価格 |
| quantity | Integer | | 取引株数 |
| pnl | Float | | 損益 |
| exit_reason | String(30) | | STOP_LOSS / SIGNAL_SELL / END_OF_PERIOD |

---

### 1.5 src/data/market_data.py — 市場データ

**責務**: yfinanceによるOHLCV取得とDB管理

| 関数 | 説明 |
|------|------|
| `fetch_ohlcv(symbol, start, end)` | yfinanceから権利修正済みOHLCV取得 |
| `upsert_ohlcv(symbol, df)` | DBにOHLCVをupsert（既存行は更新） |
| `update_symbol(symbol, years)` | 指定年数分のデータを取得・保存 |
| `load_ohlcv(symbol, limit)` | DBからOHLCVをDataFrameで読み込み |

**銘柄コード変換**: `7203` → `7203.T`（yfinance東証形式）

**MultiIndex カラム対応**: yfinance 1.x の `download()` は単一銘柄でも
MultiIndex カラム（例: `('Close', '7203.T')`）を返すため、
`isinstance(df.columns, pd.MultiIndex)` で判定し
`df.columns.get_level_values(0)` でフラット化してから列名を小文字化する。

**upsertロジック**:
```
1. 全既存レコードを一括SELECT（N+1を排除）
2. 日付をキーにした辞書（date → OHLCVオブジェクト）を構築
3. 各行を辞書と照合:
     既存あり → UPDATE（全カラム更新）
     既存なし → INSERT
```

**`load_ohlcv(symbol, limit)` の読み込み順序**:
- `ORDER BY date DESC LIMIT limit` で最新N件を取得してから `reversed()` で昇順に変換
- `ORDER BY date ASC LIMIT limit` は古い方からlimit件になり指標計算に支障が出るため使用しない

---

### 1.6 src/strategy/indicators.py — テクニカル指標

**責務**: OHLCVへの指標付加と機械学習用特徴量生成

> **実装方針**: `pandas-ta==0.3.14b0` は PyPI で Python 3.11 向けに配布されていないため、
> 全指標を `pandas` のみで直接計算する（外部テクニカル指標ライブラリへの依存を排除）。
> モジュール内のプライベート関数 `_sma` / `_rsi` / `_bbands` / `_macd` がそれぞれ実装を担う。

#### `compute_indicators(df)` — 指標計算

| 指標 | カラム名 | パラメータ | 計算方法 |
|------|---------|-----------|---------|
| 短期移動平均 | `ma5` | 5日 | `rolling(5).mean()` |
| 中期移動平均 | `ma25` | 25日 | `rolling(25).mean()` |
| 長期移動平均 | `ma75` | 75日 | `rolling(75).mean()` |
| RSI | `rsi` | 14日 | Wilder の指数移動平均（`ewm(com=length-1)`）による gain/loss 比 |
| ボリンジャー下限 | `bb_lower` | 20日・2σ | `mid - std * rolling(20).std(ddof=0)` |
| ボリンジャー中央 | `bb_mid` | 20日 | `rolling(20).mean()` |
| ボリンジャー上限 | `bb_upper` | 20日・2σ | `mid + std * rolling(20).std(ddof=0)` |
| MACD | `macd` | fast=12 / slow=26 | `ewm(12) - ewm(26)`（adjust=False） |
| MACDヒストグラム | `macd_hist` | | `macd - macd_signal` |
| MACDシグナル | `macd_signal` | signal=9 | `macd.ewm(span=9, adjust=False).mean()` |
| 前日比 | `returns` | | `pct_change(fill_method=None)` |
| 出来高移動平均 | `volume_ma20` | 20日 | `rolling(20).mean()` |
| 出来高比率 | `volume_ratio` | | volume / volume_ma20 |

> `pct_change()` 系の呼び出しは pandas 2.x の `fill_method` デフォルト変更に伴う
> `FutureWarning` を避けるため、明示的に `fill_method=None` を指定する。

#### `build_features(df)` — 機械学習特徴量

`compute_indicators` の結果に追加で計算（ラベルは付与せず特徴量のみ）:

| 特徴量 | 説明 |
|-------|------|
| `ma_cross_sm` | ma5 - ma25（短中クロス幅） |
| `ma_cross_ml` | ma25 - ma75（中長クロス幅） |
| `bb_pct` | バンド内の位置（0=下限、1=上限） |
| `price_momentum_5` | 5日前比騰落率 |
| `price_momentum_20` | 20日前比騰落率 |

特徴量はすべて過去データのみから計算され、`dropna` は `FEATURE_COLS` 列に対してのみ行うため、予測対象の最新行も保持される。

**FEATURE_COLS**（モデルへの入力10項目）:
```python
["ma_cross_sm", "ma_cross_ml", "rsi", "macd", "macd_hist",
 "bb_pct", "volume_ratio", "price_momentum_5", "price_momentum_20", "returns"]
```

---

### 1.7 src/strategy/labeling.py — トリプルバリア法ラベリング

**責務**: López de Prado のトリプルバリア法によるラベル生成とサンプル重み計算

| 関数 | 説明 |
|------|------|
| `get_daily_volatility(close, span)` | 日次リターンの指数加重標準偏差（動的ボラティリティ） |
| `triple_barrier_labels(df, pt_mult, sl_mult, max_holding, vol_span)` | 各行にラベルとイベント終了位置を付与 |
| `get_sample_weights(t_ends)` | ラベル重なりに基づくサンプル一意性重み |
| `build_training_set(df)` | 特徴量・ラベル・重みをまとめて生成（X, y, weights） |

#### トリプルバリアの3つの境界

```
エントリー価格 P、ボラティリティ v に対して
  上バリア（利益確定）  : P × (1 + tb_profit_mult × v)
  下バリア（損切り）    : P × (1 - tb_stop_mult × v)
  垂直バリア（時間切れ） : エントリーから tb_max_holding 日後

ラベル付け:
  上バリアに先に到達       → 1（利益が出る買い場）
  下バリアに先に到達       → 0（損切りになる）
  時間切れ                → 最終リターンの符号（>0で1、それ以外0）
```

#### サンプル一意性重み

```
各バーで同時進行中のイベント数（concurrency）を集計
イベントiの重み = そのイベント期間における 1/concurrency の平均
  → 多数のラベルが時間的に重なる期間のサンプルは情報重複が大きいため
    重みを下げ、IID仮定の崩れによる過学習を抑制する
```

---

### 1.8 src/strategy/ml_model.py — 機械学習モデル

**責務**: LightGBMモデルの学習・保存・推論

> **モデル選定根拠**: 2025-2026年の比較研究で、日足スイングのようにサンプル数が
> 限られる表形式金融データでは、勾配ブースティング（LightGBM）がTransformer系
> 深層学習（PatchTST/TFT/TimesNet等）より優位かつ計算効率・解釈性で勝るとされる。
> モデル本体はLightGBMを維持し、ラベリングをトリプルバリア法へ強化した。

#### `train(df, trigger, save)` — 学習フロー

`trigger` が指定された場合（`"weekly_schedule"` / `"manual"`）はCV結果・特徴量重要度を `model_metrics` テーブルに保存する。バックテスト内での学習時は `trigger=None` で呼び出してDBへの記録を抑制する。

`save=False` を指定するとモデルを `models/lgb_model.pkl` に書き込まない（バックテスト内での呼び出しで使用し、本番モデルの上書きを防ぐ）。デフォルトは `save=True`。



```
1. build_training_set() で特徴量・トリプルバリアラベル・サンプル重みを生成
2. データが100件未満 → ValueError
3. TimeSeriesSplit(n_splits=5) でCV
   - 各foldでLGBMClassifier学習（sample_weight付き）
   - early_stopping(20) で最適ステップ数を収集（best_n_list）
4. CV精度（平均・標準偏差）をログ出力
5. 各foldの最適ステップ数を平均して best_n_estimators を決定
6. 全データ＋サンプル重みで最終モデルを学習
7. models/lgb_model.pkl に保存
```

**LightGBMパラメータ**:

| パラメータ | 値 |
|-----------|-----|
| n_estimators | 200（early stoppingで調整） |
| learning_rate | 0.05 |
| num_leaves | 31 |
| random_state | 42 |

#### `predict_proba(model, df)` — 推論

```
入力: OHLCV DataFrame（またはすでに特徴量列を持つDataFrame）
  → FEATURE_COLS[0]（ma_cross_sm）が既に存在する場合はbuild_features()をスキップ
    （signal.py から build_features 済みのdfを渡す場合に二重計算を防ぐ）
  → 最新行のみ抽出
  → model.predict_proba() で上昇確率取得
出力: float（0.0〜1.0）
  ※ データ不足時は 0.5（中立）を返す
```

---

### 1.9 src/strategy/signal.py — シグナル生成

**責務**: ルールベース＋MLスコアを統合して売買シグナルを生成

**`generate()` の前処理**:
- `build_features(df)` を呼び出してテクニカル指標＋ML用特徴量を一括生成
- `compute_indicators()` ではなく `build_features()` を使うことで、`predict_proba()` への渡し時に二重計算を防ぐ

#### `compute_rule_score(df)` — ルールベーススコア計算

> **旧名 `_rule_score`**: バックテストエンジンから呼び出す必要があるため公開関数に昇格。



| 条件 | スコア変動 |
|------|----------|
| ゴールデンクロス（ma5がma25を上抜け） | +0.4 |
| デッドクロス（ma5がma25を下抜け） | -0.4 |
| RSI < 30（売られすぎ） | +0.3 |
| RSI > 70（買われすぎ） | -0.3 |
| 終値 < ボリンジャー下限 | +0.2 |
| 終値 > ボリンジャー上限 | -0.2 |
| MACDヒストグラム正転換 | +0.1 |
| MACDヒストグラム負転換 | -0.1 |

スコア範囲: -1.0 〜 +1.0（`max(-1.0, min(1.0, score))` でクリップ）

#### 統合スコア計算

```
MLスコア変換: (上昇確率 - 0.5) × 2   ← -1.0〜+1.0 に正規化

統合スコア = ルールスコア × 0.5 + MLスコア × 0.5

統合スコア >= +0.25 → BUY
統合スコア <= -0.25 → SELL
それ以外            → HOLD
```

> **閾値はスコアの実効レンジに合わせる**: ルールスコア（理論±1.0）とMLスコアを
> 0.5ずつ重み付き平均するため、統合スコアの実測レンジは約±0.3に収まる
> （実データ: 平均≈0、標準偏差≈0.11、最大≈0.32）。閾値 0.25 は平均+約2.2σで、
> 年2〜3往復程度のスイング頻度になる。理論上限1.0を前提とした旧値0.6は実効レンジ外で
> 一度も発火しなかった。バックテスト完了時の `[診断]` ログ（`_log_score_diagnostics`）で
> 候補閾値ごとのシグナル発生回数を確認し、銘柄・期間に応じて調整できる。

---

### 1.10 src/risk/manager.py — リスク管理

**責務**: 発注前のバリデーションと損切り判定

**クラス: `RiskManager`**

| メソッド | 説明 |
|---------|------|
| `can_place_order()` | 日次注文数上限・当日損失上限の統合チェック。`(ok, reason)` を返す |
| `reset_daily_counters()` | 日次カウンタリセット（`_daily_order_count` と `_daily_loss_yen` 両方） |
| `record_loss(pnl)` | 損益を記録。負のpnlのみ `_daily_loss_yen` に累積する |
| `is_daily_loss_limit_reached()` | 当日損失上限チェック。`(over_limit, reason)` を返す |
| `increment_order_count()` | 注文カウンタをインクリメント |
| `calc_position_size(symbol, price, cash)` | 購入可能株数計算 |
| `check_max_positions()` | 最大保有銘柄数チェック |
| `check_sector_concentration(sector)` | セクター集中率チェック |
| `should_stop_loss(symbol, price)` | 損切りライン判定 |
| `validate_buy(symbol, price, cash, sector)` | 買い注文の総合バリデーション |

#### 当日損失上限チェック

```
_daily_loss_yen: SELL 約定時に record_loss(pnl) で累積（損失のみ）
  ↓
can_place_order() で is_daily_loss_limit_reached() を呼び出し
  → _daily_loss_yen >= max_daily_loss → (False, "当日損失上限(N円)に達しました")
  → max_daily_loss == 0 → チェック無効（デフォルト動作）

毎営業日 8:25（scheduler.py の risk_reset ジョブ）に reset_daily_counters() を呼び、
_daily_loss_yen と _daily_order_count の両方をリセットする。
```

#### validate_buy() の結線

`validate_buy(symbol, price, cash, sector)` は `signal_scan`（ペーパーモード）と
`morning_execution`（ライブモード）のBUYパスから呼び出され、`check_max_positions()` と
`check_sector_concentration(sector)` を発注前に必ず通過させる。セクターは
`watchlist_store.get_sectors()`（ウォッチリスト追加時に `market_data.lookup_sector()` で
yfinanceから自動取得・保存）から取得する。取得に失敗した場合は空文字となり、
セクター集中チェックはスキップされる（`check_max_positions()` は引き続き有効）。

#### 購入株数計算

```
上限金額 = 現金残高 × max_position_ratio（デフォルト20%）
最大単元数 = floor(上限金額 / (株価 × 100))
購入株数 = 最大単元数 × 100
```

#### 損切り判定

```
損益率 = (現在値 - 平均取得単価) / 平均取得単価
損益率 <= stop_loss_pct（デフォルト-5%）→ 損切り発動
```

---

### 1.11 src/execution/order_manager.py — 発注管理

**責務**: 指値注文の発注・ポジション更新・タイムアウト管理

**クラス: `OrderManager`**

**スレッドセーフティ**:
- `_pending_orders` 辞書へのアクセスはすべて `self._orders_lock`（`threading.Lock`）で保護
- `_cancel_timeout_timer()` と `_timeout_cancel()` の両方がロックを取得してから辞書を操作

**日次注文上限・損失上限チェック**:
- `buy()` / `sell()` の先頭で `risk.can_place_order()` を呼び出し、注文数/損失上限のいずれかに達していれば即 `None` を返す
- `increment_order_count()` はチェック後のみ呼び出す

**二重発注防止（ライブモードのみ）**:
- `buy()` / `sell()` でライブモード時に `_has_pending_order(symbol)` を呼び出す
- DBの trades テーブルで同銘柄の PENDING 注文が存在する場合はスキップ

**約定イベントの冪等処理**:
- `on_order_event()` は約定通知（OrderState=5）受信時にDB上のステータスを確認
- trade が None（不明な OrderID）または既に FILLED → スキップ（二重処理防止）

**起動時API同期 `sync_on_startup()`**:
- ライブモード起動時に kabuステーション API から現在の注文一覧を取得
- DB上の PENDING 注文で API に存在しないものを CANCELLED に更新
- 通信断などで宙に浮いたステータスをリカバリする

#### ペーパーモードとliveモードの切り替え

```python
self._is_paper = config["trading"]["mode"] == "paper"
```

| モード | buy()/sell() の動作 |
|--------|-------------------|
| paper | DBに status=FILLED / filled_at=今 で即時記録。ポジションも即時更新。 |
| live | kabuステーションAPIで発注（`Result==0` かつ `OrderId` 非空を検証）→ WebSocketで約定確認後にポジション更新 |

**ライブモードでの発注結果検証**:
```
result = client.send_order(order)
result["Result"] != 0  → エラーログ・None 返却
result["OrderId"] が空  → エラーログ・None 返却
```

#### 注文IDの生成

| モード | 形式 |
|--------|------|
| paper | `PAPER-BUY-{symbol}-{uuid8桁}` |
| live | kabuステーションAPIが返す OrderId |

#### タイムアウトキャンセル

```
発注と同時に threading.Timer を設定
  → order_timeout_seconds（デフォルト300秒）後に cancel_order() を呼び出し
  → 約定確認イベントを受信した場合はタイマーをキャンセル
```

#### ポジション更新ロジック（BUY時）

```
既存ポジションあり:
  新平均取得単価 = (既存単価×既存株数 + 新単価×新株数) / (既存株数+新株数)
  株数 += 新株数

既存ポジションなし:
  新規Positionレコードを作成
```

#### ポジション更新ロジック（SELL時）

```
損益 = (売却価格 - 平均取得単価) × 売却株数
株数 -= 売却株数（最小0）
損益を対応するTradeレコードに記録（order_id で一意に特定）
```

> `order_id` を使ってTradeレコードを特定することで、同一銘柄の並行売却時に
> `symbol + side` による曖昧な照合が発生しないようにしている。

---

### 1.12 src/dashboard/app.py — Webダッシュボード

**責務**: FastAPIによるダッシュボードのREST API提供

#### APIエンドポイント一覧

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `/` | ダッシュボードHTML（`frontend/index.html`） |
| GET | `/api/status` | システム稼働状態 |
| GET | `/api/watchlist` | ウォッチリスト銘柄一覧（コード・会社名） |
| POST | `/api/watchlist` | ウォッチリストに銘柄追加（`{code, name}`、既存コードなら会社名更新） |
| DELETE | `/api/watchlist/{code}` | ウォッチリストから銘柄削除 |
| GET | `/api/symbol_names` | 銘柄コード→会社名マッピング（ダッシュボード表示用） |
| GET | `/api/positions` | 保有ポジション一覧（含み損益・現在値含む） |
| GET | `/api/trades?limit=50` | 取引履歴 |
| GET | `/api/signals?limit=50` | シグナル履歴 |
| GET | `/api/pnl_chart` | 累積損益時系列（Chart.js用） |
| GET | `/api/pnl/daily?days=90` | 日次損益と累積損益（棒グラフ用） |
| GET | `/api/pnl/enhanced_summary` | 今日/MTD/YTD/含み損益/シャープ/最大DD |
| GET | `/api/model/metrics?limit=20` | MLモデル学習履歴 |
| GET | `/api/model/latest` | 最新モデルの精度・特徴量重要度 |
| POST | `/api/model/retrain` | MLモデル手動再学習 |
| POST | `/api/data/update` | ウォッチリスト全銘柄の過去データを手動取得（yfinance） |
| POST | `/api/backtest/run` | バックテスト実行（run_id を返す） |
| GET | `/api/backtest/runs?limit=20` | バックテスト実行履歴 |
| GET | `/api/backtest/{run_id}` | バックテスト詳細（エクイティカーブ＋取引一覧） |
| POST | `/api/emergency_close` | 緊急全ポジション決済（要トークン認証） |

#### `/api/positions` レスポンスフィールド

| フィールド | 説明 |
|-----------|------|
| symbol | 銘柄コード |
| quantity | 保有株数 |
| avg_cost | 平均取得単価 |
| sector | セクター |
| opened_at | 最初の取得日時 |
| latest_price | 最新終値（OHLCVテーブルより） |
| unrealized_pnl | 含み損益 = (latest_price - avg_cost) × quantity |
| return_pct | リターン率 = (latest_price - avg_cost) / avg_cost |

#### `/api/pnl/enhanced_summary` レスポンス例

```json
{
  "total_pnl": 50000.0,
  "today_pnl": 1500.0,
  "mtd_pnl": 8000.0,
  "ytd_pnl": 50000.0,
  "total_unrealized_pnl": 3200.0,
  "win_count": 25,
  "loss_count": 10,
  "win_rate": 0.714,
  "max_drawdown": -0.082,
  "sharpe_ratio": 1.45
}
```

#### `/api/pnl/daily` レスポンス例

```json
[
  {"date": "2026-06-01", "daily_pnl": 3000.0, "cumulative_pnl": 45000.0},
  {"date": "2026-06-02", "daily_pnl": -1500.0, "cumulative_pnl": 43500.0}
]
```

- `days` パラメータで直近N日分に絞り込む（デフォルト90日）
- 日次損益はその日の全SELL取引の `pnl` 合計
- `cumulative_pnl` はリクエスト全期間（days=∞相当）からの累積ではなく、返却日付範囲内の累積

#### 統計ヘルパー関数

| 関数 | 説明 |
|------|------|
| `_compute_max_drawdown(cumulative_values)` | 累積損益リストから最大ドローダウン率を返す（負値、例: -0.08 = -8%） |
| `_compute_sharpe(daily_pnl)` | 日次損益リストから年率換算シャープレシオを返す（√252 でスケール） |

**最大ドローダウン計算**:
```
peak = 累積損益の現時点最大値
drawdown = (current - peak) / peak  ← peak > 0 の場合のみ
max_drawdown = min(全drawdown値)
```

**シャープレシオ計算**（無リスク金利=0として近似）:
```
sharpe = mean(daily_pnl) / std(daily_pnl) × √252
```

#### 緊急決済エンドポイントの認証

- `X-Emergency-Token` ヘッダーが必須（FastAPI `Depends` で強制）
- トークンは起動時に `config.yaml` の `dashboard.emergency_token` から読み込む（空の場合はランダム生成）
- 起動ログに `緊急決済トークン (X-Emergency-Token ヘッダーに設定): XXXX` として出力
- フロントエンドの決済ボタン押下時に `prompt()` でトークン入力を求め、ヘッダーとして送信

---

### 1.13 src/backtest/engine.py — バックテストエンジン

**責務**: ウォークフォワード方式でのバックテスト実行とDB保存

#### `run_backtest(symbol, start, end, initial_capital, use_ml)` — 実行フロー

```
1. load_ohlcv(symbol, limit=2000) で全履歴を取得
2. build_features(full_df) で特徴量を一括計算（ルックアヘッドバイアスなし）
3. use_ml=True の場合: start以前のデータのみでMLモデルを学習（trigger=None）
4. 各テスト日（start〜end）についてループ:
     a. featured_df[date <= dt] でその日時点のデータにスライス
     b. ポジション保有中: 損切りライン（-5%）チェック
     c. シグナルスコア計算（compute_rule_score + predict_proba）
     d. BUYシグナル: 余力から購入株数を計算して買い
        SELLシグナル: 保有中なら決済
5. 最終日にポジション残存があれば END_OF_PERIOD で強制決済
6. BacktestRun（サマリー＋エクイティカーブ）をDBに保存
7. BacktestTradeRecord（全取引）をDBに保存
8. run_id を返す
```

**ルックアヘッドバイアス防止**:
- `build_features()` は全期間で一括計算しているが、各日のシグナル判定は `featured_df[featured_df.index <= dt]` でスライスした上で最新行のみ使用する
- ML学習時は `full_df[full_df.index.date < start]` のデータのみ使用

#### 統計計算（バックテスト専用）

| 関数 | 説明 |
|------|------|
| `_max_drawdown(equity_curve, initial_capital)` | 資産曲線から最大ドローダウン率を計算 |
| `_sharpe_ratio(equity_curve)` | 資産曲線の日次リターンから年率シャープレシオを計算 |

---

## 2. シーケンス図

### 2.1 通常の売買フロー（スイングトレード）

```
16:00（平日）
    │
    ▼
data_update() 実行 ── yfinanceで当日分のOHLCVをDBへ反映
    │
    ▼
16:20（平日。data_update完了後であること。順序を入れ替えると前日終値で
        シグナルを生成してしまうため、signal_scanは必ずdata_updateより後にする）
    │
    ▼
signal_scan() 実行
    │
    ├─ load_ohlcv(symbol) ── DB（当日分反映済み）
    │
    ├─ gen_signal(symbol, df, model)
    │     ├─ build_features()        ← compute_indicators() + ML特徴量を一括生成
    │     ├─ compute_rule_score()    ← 旧 _rule_score()、バックテストからも呼び出し可能
    │     └─ ml_model.predict_proba()  ← 特徴量列が既存なので再計算スキップ
    │
    ├─ シグナルをDBに保存（翌朝の morning_execution で参照）
    │
    └─ [ペーパーモード] 当日終値で即時シミュレート:
          BUYシグナル → watchlist_store.get_sectors() でセクター取得
                      → risk.validate_buy()（max_positions/セクター集中チェック）
                      → risk.calc_position_size() → order_mgr.buy() → DB FILLED
          SELLシグナル → _get_position_qty() → order_mgr.sell() → DB FILLED

9:05（翌平日・ライブモードのみ）
    │
    ▼
morning_execution() 実行
    │
    ├─ 前日シグナル（20時間以内）をDBから取得・重複銘柄を除外
    │
    ├─ SELL シグナル優先ループ（余力確保のため先行実行）
    │     └─ _get_position_qty() > 0 → client.get_board() → order_mgr.sell()
    │
    └─ BUY シグナルループ
          └─ client.get_wallet() で余力確認
             → client.get_board() で現在値取得
             → watchlist_store.get_sectors() でセクター取得
             → risk.validate_buy()（max_positions/セクター集中チェック）
             → risk.calc_position_size()
             → order_mgr.buy(symbol, price, qty, sector=sector)
                  │
                  ├─ _has_pending_order() で二重発注チェック
                  ├─ risk.can_place_order() で注文数/損失上限チェック
                  ├─ kabuAPI で発注（Result==0・OrderId 非空を検証）
                  └─ タイムアウトタイマーをセット
                         │
                         └─ WebSocket OrderEvent受信（OrderState=5）
                               → 冪等チェック（既に FILLED ならスキップ）
                               → タイマーキャンセル
                               → status = FILLED
                               → _update_position_from_fill()
                               → risk.record_loss(pnl)（SELL時のみ）
```

### 2.2 損切りフロー

```
9:00〜15:30（5分毎）
    │
    ▼
stop_loss_check() 実行
    │
    ├─ client.get_board(symbol) で現在値取得
    │
    └─ risk.should_stop_loss(symbol, price)
          損益率 <= -5%?
                YES → order_mgr.sell()
                     └─ alert("損切り実行", ...)
```

---

## 3. エラー処理方針

| 状況 | 対応 |
|------|------|
| kabuステーション接続失敗（起動時） | ログ警告を出してペーパーモードで継続 |
| ライブモード環境変数未設定 | `CONFIRM_LIVE_TRADING=true` がなければ起動時に `sys.exit(1)` |
| 起動時PENDING注文不明 | `sync_on_startup()` でAPIと照合し未存在注文を CANCELLED に更新 |
| APIトークン更新失敗 | LINE Notifyでアラート送信 |
| WebSocket切断 | 自動再接続（指数バックオフ、最大60秒） |
| 注文発注失敗（Result≠0 / OrderId欠落） | エラーログを記録して None を返す、処理スキップ |
| 二重発注 | _has_pending_order() でDBチェック、PENDING 存在時はスキップ |
| WebSocket約定通知の重複 | on_order_event() で FILLED チェックにより冪等保証 |
| 当日損失上限到達 | can_place_order() が False を返し新規発注を停止 |
| データ取得失敗（yfinance） | ログ警告、当該銘柄をスキップ |
| ML推論失敗 | ログ警告、MLスコアを0.0（中立）として処理を継続 |
| DB操作エラー | SQLAlchemyの例外をcatch、ログ記録 |

---

## 4. 設定ファイル詳細（config.yaml）

```yaml
kabu_station:
  base_url: "http://localhost:18080/kabusapi"  # kabuステーションURL
  password: ""                                  # 非推奨: .env の KABU_API_PASSWORD を使用
  token_refresh_time: "08:30"                  # トークン自動更新時刻

trading:
  mode: "paper"            # paper / live
  max_positions: 5         # 最大同時保有銘柄数
  max_position_ratio: 0.25 # 1銘柄最大投資比率
  stop_loss_pct: -0.07     # 損切りライン
  max_sector_ratio: 0.40   # 同一セクター集中率上限
  order_timeout_seconds: 300  # 未約定キャンセルまでの秒数
  daily_order_limit: 100   # 1日の最大注文数
  max_daily_loss: 30000    # 当日損失上限（円）。0 で無効
  paper_initial_capital: 500000  # ペーパーの初期資金（発注サイズ算定の基準）
  # ※ max_positions/max_position_ratio/stop_loss_pct/max_sector_ratio/max_daily_loss は
  #    アクティブな risk_profile の値で起動時・切替時に上書きされる（§4.3）
  # ウォッチリストは watchlist.json で管理（本ファイルには含めない）
  ...

# リスクプロファイル（§4.3 参照）。low_risk / high_risk の2択
active_risk_profile: "low_risk"
risk_profiles:
  low_risk:  { max_position_ratio: 0.12, stop_loss_pct: -0.04, max_positions: 6, max_sector_ratio: 0.30, max_daily_loss: 15000, buy_threshold: 0.32, sell_threshold: -0.32 }
  high_risk: { max_position_ratio: 0.35, stop_loss_pct: -0.10, max_positions: 4, max_sector_ratio: 0.55, max_daily_loss: 60000, buy_threshold: 0.18, sell_threshold: -0.18 }

strategy:
  ma_short: 5              # 短期MA日数
  ma_mid: 25               # 中期MA日数
  ma_long: 75              # 長期MA日数
  rsi_period: 14           # RSI計算期間
  rsi_oversold: 30         # RSI売られすぎ閾値
  rsi_overbought: 70       # RSI買われすぎ閾値
  bb_period: 20            # ボリンジャー期間
  bb_std: 2.0              # ボリンジャー標準偏差
  ml_weight: 0.5           # MLシグナル重み
  rule_weight: 0.5         # ルールシグナル重み
  buy_threshold: 0.25      # 買いシグナル閾値（risk_profile で上書きされる）
  sell_threshold: -0.25    # 売りシグナル閾値（risk_profile で上書きされる）

data:
  db_path: "data/kabu_auto.db"
  backup_dir: "data/backups"
  history_years: 7

dashboard:
  host: "0.0.0.0"
  port: 8080
  emergency_token: ""  # 空の場合は起動時にランダム生成してログに出力

alerts:
  line_notify_token: ""    # LINE Notifyトークン（任意）
  email: ""                # メール通知先（任意）

logging:
  level: "INFO"
  file: "data/kabu_auto.log"
```

### 4.1 .env（機密情報、`.gitignore`済み）

```
KABU_API_PASSWORD=kabuステーションのログインパスワード
```

`main.py` 起動時に `load_dotenv()` で読み込まれ、`config.py` の `_apply_env_overrides()` が
`kabu_station.password` をこの値で上書きする。`.env` が無い環境では `config.yaml` の
`password`（通常は空文字）がそのまま使われる。

### 4.2 watchlist.json（ウォッチリスト、GUIから編集可）

```json
[
  { "code": "8306", "name": "三菱UFJフィナンシャル・グループ" },
  { "code": "7203", "name": "トヨタ自動車" }
]
```

`src/core/watchlist.py` が読み書きを担当する。ダッシュボードの `/api/watchlist`（POST/DELETE）
経由で更新すると即座にこのファイルへ書き込まれ、`main.py` のスケジュールジョブは
`watchlist_store.get_codes()` を都度呼ぶため再起動なしで反映される。

### 4.3 リスクプロファイル切替（risk_profile.json）

**目的**: 日次の損益を見ながら「ハイリスク・ハイリターン」⇔「ローリスク・ローリターン」の
2択をダッシュボードから切り替えられるようにする。

**仕組み**（`src/core/risk_profile.py`）:
- `config.yaml` の `risk_profiles` に `low_risk`（ローリスク・ローリターン）と
  `high_risk`（ハイリスク・ハイリターン）の2プロファイルを定義
- アクティブなプロファイル名は `risk_profile.json`（`{"active": "low_risk"}`）に永続化。
  既定は `low_risk`（live運用へ進んだ場合の安全側デフォルト）
- 切替時、プロファイルの各値を `config` の trading / strategy セクション辞書へ**インプレース反映**する。
  `RiskManager` は trading 辞書の参照を保持し、strategy 側は `generate()` ごとに読み直すため、
  発注サイズ（`max_position_ratio`）・損切り幅（`stop_loss_pct`）・最大保有数（`max_positions`）・
  セクター集中（`max_sector_ratio`）・当日損失上限（`max_daily_loss`）・売買閾値
  （`buy_threshold`/`sell_threshold`）に即座に反映される
- 起動時 `main.py` が `risk_profile_store.load()` を呼び、永続化された（無ければ `active_risk_profile`）
  プロファイルを適用する

**API**: `GET /api/risk_profile`（現在値と全プロファイル）/ `POST /api/risk_profile {name}`（切替）。
`GET /api/status` にも `risk_profile`（アクティブ名）を含める。

**設計方針**: 単に1パラメータを動かすのではなく、ポジションサイズ・損切り幅・分散度・
シグナル選別の厳しさ・損失許容額の5軸を「リスクを取る方向」に一貫して連動させている
（ローリスク側は小さく張って早めに損切り・広く分散・強いシグナルのみ採用、ハイリスク側は
大きく集中投資して損切りを広げ取引頻度も上げる）。

| プロファイル | 性格 | max_position_ratio | stop_loss_pct | max_positions | max_sector_ratio | max_daily_loss | 売買閾値 |
|---|---|---|---|---|---|---|---|
| `low_risk` | ローリスク・ローリターン（既定） | 12% | -4% | 6 | 30% | ¥15,000 | ±0.32 |
| `high_risk` | ハイリスク・ハイリターン | 35% | -10% | 4 | 55% | ¥60,000 | ±0.18 |

### 4.4 日時のタイムゾーン方針

DBに保存する全ての `DateTime`（`Trade.filled_at`・`Position.opened_at/updated_at`・
`Signal.generated_at`・`ModelMetrics.trained_at` など）は **JST naive**（`datetime.now()`）で統一する。
スケジューラ・発注・ML はいずれも JST であり、`morning_execution` の cutoff 比較
（`datetime.now() - timedelta(hours=20)`）と `Signal.generated_at` の基準を一致させないと、
前日シグナルを9時間ぶん取りこぼしてライブ自動売買が成立しない。ダッシュボードの当日損益判定
（`enhanced_summary`）も `datetime.now().date()` を用い、フロントは JST naive をそのまま
ローカル時刻として解釈する（`generated_at` に末尾 `Z` を付けない）。

---

## 5. 技術スタック詳細

| 分類 | ライブラリ | バージョン | 用途 |
|------|-----------|----------|------|
| API通信 | requests | 2.31.0 | REST APIクライアント |
| WebSocket | websocket-client | 1.7.0 | リアルタイム価格受信 |
| データ処理 | pandas | 2.1.4 | OHLCV操作・特徴量生成 |
| 数値計算 | numpy | 1.26.2 | スコア計算 |
| テクニカル指標 | pandas（自前実装） | - | MA/RSI/BB/MACD（`src/strategy/indicators.py`） |
| 機械学習 | lightgbm | 4.1.0 | 分類モデル |
| ML前処理 | scikit-learn | 1.3.2 | TimeSeriesSplit・精度評価 |
| DB | sqlalchemy | 2.0.23 | ORM（SQLite） |
| Webサーバ | fastapi | 0.104.1 | REST API |
| ASGI | uvicorn | 0.24.0 | FastAPIサーバ |
| スケジューラ | APScheduler | 3.10.4 | 定時処理 |
| ログ | loguru | 0.7.2 | ローテーションログ |
| 設定 | PyYAML | 6.0.1 | config.yaml読み込み |
| 株価データ | yfinance | ※最新 | 過去OHLCV取得 |
| タイムゾーン | pytz | ※最新 | Asia/Tokyo管理 |

---

## 6. ファイル・データ管理

| ファイル | 場所 | 説明 |
|---------|------|------|
| 設定ファイル | config.yaml | 起動時に読み込み |
| データベース | data/kabu_auto.db | SQLite（WAL） |
| ログ | data/kabu_auto.log | 30日分ローテーション |
| DBバックアップ | data/backups/kabu_auto_YYYY-MM-DD.db | 30日分保持 |
| MLモデル | models/lgb_model.pkl | pickle形式 |
