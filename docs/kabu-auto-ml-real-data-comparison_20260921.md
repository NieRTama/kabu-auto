# 実データでの5モデル比較・3戦略比較 報告書 v2（方法論是正版）

実施日: 2026-09-20〜09-21
実施者: Claude（分析担当エージェント）
リポジトリ: C:\Users\garnet\kabu-auto（コミットなし・リポジトリ配下への書き込みなし）
前版: `real-evaluation-report.md`（Opus 5のレビューで方法論上の誤りを複数検出）

---

## 0. 前回報告書との違い（要約）

Opus 5のレビュー指摘（是正1〜7）を反映し、**前回の結論の多くが覆った**。

| 項目 | 前回 | 今回（v2） |
|---|---|---|
| 使用した設定 | `config.yaml`の素の値（`cfg.load()`のみ） | **`risk_profile.json`(high_risk)適用後の本番実効値**（`buy_threshold=0.08`, `sell_threshold=-0.08`, `stop_loss_pct=-0.10`, `max_position_ratio=0.50`, `max_positions=8`, `max_sector_ratio=0.55`） |
| イベント総数 | 5,484件（決着5,464件） | **12,033件（決着11,981件）** ※ラベル定義自体が変わったため単純比較不可 |
| current_lightgbm AUC平均 | 0.4855（前回は「chance未満」と表現） | **0.5018**（t=0.380, p=0.7231）。**0.5と統計的に区別できない**（優位でも劣位でもない） |
| 3戦略比較の期間設計 | 「直近1年」「直近2年」＝**入れ子**（2つの独立確認になっていない） | **3つの独立な非重複1年期間**（A: 2023-09-18〜2024-09-18 / B: 2024-09-18〜2025-09-18 / C: 2025-09-18〜2026-09-18） |
| weighted_blendの優劣 | 「両期間ともweighted_blendが最も優れている」 | **3期間中1期間（B）のみ優位。A・Cではrule_only系（対照実験含む）に明確に劣る** |
| 対照実験 | 無し | **rule_only@0.50（是正4）を追加。weighted_blendがこの対照にも勝てたのは3期間中1期間のみ** |
| ウォームアップ影響 | 言及なし | **各期間とも約49%がウォームアップ期間（MLは常にNone）であることを明記** |
| 本番model_metricsとの比較 | 「差の大きさ＝リークの分」と数値で言い切り | **方向性の指摘のみに訂正**（リークを含むコードパスの存在は明白だが、差の大きさをそれだけに帰属できない） |
| purge/embargoの表現 | 「purge/embargo付き分割」 | **「purge付き分割（embargoは今回0を使用）」に訂正** |

**結論の変化の核心**: 前回はrisk_profile未適用（`buy_threshold=0.25`, `sell_threshold=-0.25`など）で測定していたため、(a) ラベル生成自体が本番と別物になっていた、(b) `weighted_blend`の実効ゲートが「かなり厳しいフィルター」として働き、それが`weighted_blend`優位に見えた主因だった可能性が高い。本番実効の`buy_threshold=0.08`はゲートが大幅に緩く、weighted_blendの「フィルター効果」仮説そのものが前提から崩れた。**是正後は、weighted_blendが一貫して優れているという根拠は無くなった。**

---

## 1. 是正1: risk_profile適用の詳細

`main.py:48`の起動順序（`cfg.load()` → `risk_profile_store.load("risk_profile.json")`）をそのまま再現した。

```
アクティブなリスクプロファイル: high_risk
strategy.buy_threshold=0.08       strategy.sell_threshold=-0.08
trading.stop_loss_pct=-0.1        trading.max_position_ratio=0.5
trading.max_positions=8           trading.max_sector_ratio=0.55
```

`risk_profile.load()`は`config.yaml`の`trading`/`strategy`セクションを**プロセス内メモリ上でのみ**上書きするだけで、`risk_profile.json`自体への書き込み（`_persist()`）は呼ばれない（`load()`内で呼んでいるのは読み込みのみ）。ソースで確認済み（`src/core/risk_profile.py:161-201`）。

`policy.config_from_settings()` / `execution.config_from_settings()`はこの上書き後の`trading`/`strategy`セクションを読むため、`PolicyConfig(stop_loss_pct=-0.1, breakeven_trigger_pct=0.02, trailing_stop_pct=0.04, sell_threshold=-0.08, max_holding_sessions=10)`が是正後の値として全段階（イベントラベル生成・3戦略比較）に一貫して使われた。

**影響の実測**: `stop_loss_pct`と`sell_threshold`が変わったことで、`dataset.build_events_multi()`が返すイベント総数が**5,484件→12,033件（決着済み5,464件→11,981件）へ2.2倍に増加**した。`sell_threshold`が`-0.25`→`-0.08`へ大幅に緩くなった（売りシグナルが出やすくなった）ことで、保有→決済のサイクルが早まり、同じOHLCV期間からより多くのイベントが切り出されたためと考えられる。**前回の5モデル比較は、本番と異なるラベル定義で「別のタスク」を解いていたことになる。**

---

## 2. 対象銘柄数・イベント総数・決着イベント数（v2）

OHLCV自体はrisk_profileの影響を受けないため、前回構築した隔離DB・OHLCVデータ（本番DBから読み取り専用でコピー済みのもの）をそのまま再利用し、ラベル付け（イベント表構築）だけをrisk_profile適用後の`policy_conf`/`costs`でやり直した。

- 対象銘柄: **62銘柄**（前回と同一。500本以上ある銘柄）
- `dataset.build_events_multi()`で作ったイベント総数: **12,033件**
  - `resolved`（決着済み）: **11,981件**
  - `immature`: 42件
  - `unfilled`: 10件
- 使用した`policy_conf`/`costs`:
  - `PolicyConfig(stop_loss_pct=-0.1, breakeven_trigger_pct=0.02, trailing_stop_pct=0.04, sell_threshold=-0.08, max_holding_sessions=10)`
  - `CostConfig(slippage_pct=0.001, commission_pct=0.0)`

---

## 3. 5モデル比較（是正版）

`n_splits=5`（`validation.calendar_folds`の拡大窓）、`persist=False`（本番DBへ書かない）で実行。所要609.6秒。

### fold横断の集計（平均）

| model_id | n_folds | n_val合計 | AUC平均 | AUC SD(fold間) | Brier平均 | Brier vs 定数(平均, raw) | logloss vs 定数(平均, raw) |
|---|---|---|---|---|---|---|---|
| constant_probability | 5 | 9560 | 0.5000 | 0.0000 | 0.247608 | -0.000382 | -0.000801 |
| majority_class | 5 | 9560 | 0.5000 | 0.0000 | 0.247625 | -0.000398 | -0.000835 |
| logistic_regression | 5 | 9560 | 0.5176 | 0.0175 | 0.247667 | -0.000440 | -0.000932 |
| small_lightgbm | 5 | 9560 | 0.5093 | 0.0117 | 0.247584 | -0.000357 | -0.020342 |
| **current_lightgbm**（現行相当） | 5 | 9560 | **0.5018** | 0.0103 | 0.247627 | -0.000400 | -0.000832 |

### 是正2: current_lightgbm（現行相当）AUCの統計的有意性

fold別AUC: `[0.4996, 0.4916, 0.4947, 0.5052, 0.5177]`

- 平均 = 0.5018、fold間SD = 0.0103、標準誤差 SE = SD/√5 = 0.0046
- 0.5に対する一標本t検定（`scipy.stats.ttest_1samp`）: **t = 0.380, p = 0.7231**
- **結論: current_lightgbm（現行相当）のAUCは0.5と統計的に区別できない（予測力が確認できない）。** 前回報告書の「chance未満」という表現は誤りであり、今回のrisk_profile是正後の再測定では平均AUCも0.5をわずかに上回っているが、p=0.72と全く有意ではなく、「上回っている」とも言えない。

他モデルも同様（**5モデルすべてが0.5との有意差なし**、`logistic_regression`が最もchanceから離れているが p=0.0876 で有意水準5%には届かない）:

| model_id | fold別AUC | 平均 | SD | SE | t | p |
|---|---|---|---|---|---|---|
| logistic_regression | [0.5016, 0.5145, 0.5141, 0.5476, 0.5105] | 0.5176 | 0.0175 | 0.0078 | 2.251 | 0.0876 |
| small_lightgbm | [0.5099, 0.4907, 0.5234, 0.5109, 0.5115] | 0.5093 | 0.0117 | 0.0052 | 1.768 | 0.1518 |
| current_lightgbm | [0.4996, 0.4916, 0.4947, 0.5052, 0.5177] | 0.5017 | 0.0103 | 0.0046 | 0.380 | 0.7231 |
| constant_probability / majority_class | 全fold0.5固定 | 0.5000 | 0.0000 | — | 定義不可(分散0) | — |

### 是正2: Brier/logloss「vs定数」の補正値（constant_probability自身の床を控除）

`constant_probability`自身の`brier_vs_constant`/`log_loss_vs_constant`は理論上0のはずだが、校正（`Calibrator`、内側foldで学習した`isotonic`等）の影響で厳密には0にならない（fold別: brier床 `{0: -0.002888, 1: 0.000963, 2: 0.001499, 3: -0.000109, 4: -0.001374}`、logloss床 `{0: -0.006003, 1: 0.001956, 2: 0.003046, 3: -0.000218, 4: -0.002787}`）。この「床」を各モデルの生値から差し引いた補正値は以下の通り:

| model_id | brier_vs_const raw平均 | brier_vs_const 補正後平均 | logloss_vs_const raw平均 | logloss_vs_const 補正後平均 |
|---|---|---|---|---|
| constant_probability | -0.000382 | 0.000000（定義上） | -0.000801 | 0.000000（定義上） |
| majority_class | -0.000398 | -0.000017 | -0.000835 | -0.000033 |
| logistic_regression | -0.000440 | -0.000058 | -0.000932 | -0.000131 |
| small_lightgbm | -0.000357 | **+0.000025** | -0.020342 | **-0.019541** |
| current_lightgbm | -0.000400 | -0.000018 | -0.000832 | -0.000031 |

補正後もcurrent_lightgbmはbrier/loglossとも定数モデルよりわずかに劣っており（マイナス）、**改善していると主張できる材料は無い**。`small_lightgbm`のloglossが大きく悪化しているのは、fold4で校正が極端な確率（0付近）を出し`_log_loss_safe`のクリップ限界近くまで振れたことが原因（fold4のみでlogloss 0.7787、他foldは0.68〜0.69）。

**fold0の寄与度**（current_lightgbmのlog_loss_vs_constant、絶対値ベース）:

| fold | n_val | log_loss_vs_constant | 寄与割合(絶対値ベース) |
|---|---|---|---|
| 0 | 2038 | -0.005334 | **44.1%** |
| 1 | 1996 | +0.001643 | 13.6% |
| 2 | 1808 | +0.002328 | 19.2% |
| 3 | 2080 | -0.000599 | 5.0% |
| 4 | 1638 | -0.002198 | 18.2% |

fold0（学習データが最も少ない初期fold、n_train=2384）が全体の44%を占めており、**平均値は初期foldの不安定さにかなり引っ張られている**。とはいえ前回（旧ラベル）ほど極端ではない（前回fold0のみでlogloss寄与97%相当、閾値0.875まで跳ね上がっていた）。今回は各foldの`threshold`も0.42〜0.67の範囲に収まっており、極端な縮退は見られない。

### degraded_reasons

**0件**（fold skip・校正縮退・モデル定数縮退・AUC未定義のいずれも発生せず）。

### 是正6: 本番`model_metrics`との比較の書き方（訂正）

本番DB`model_metrics`の直近記録値（読み取り専用で確認、参考として並べるのみ）:

| trained_at | cv_auc |
|---|---|
| 2026-09-18 | 0.5227 |
| 2026-08-17〜09-15（直近10件） | 0.5054〜0.5163 |

**前回報告書は「本番0.51〜0.52との差＝リークの分」と数値で言い切ったが、これは誤り。** ラベル定義（本分析の`dataset.build_events_multi`のpolicy方式 vs 本番`labeling.build_training_set`のトリプルバリア法）・学習データ範囲（本分析は全期間 vs 本番`load_ohlcv`既定の直近500本）・CV手法が全て異なるため、差の大きさをリークだけに帰属することはできない。

**言えるのはコード上明白な方向性のみ**: 本番の学習パイプライン（`src/strategy/ml_model.py:135-160`）は、複数銘柄を連結した行に対して単純な`TimeSeriesSplit(n_splits=5)`を適用しており（銘柄をまたいだ行順序に依存し、銘柄別の時系列性を保証しない）、かつ同じ検証fold（`eval_set=[(X_val, y_val)]`）を`early_stopping`と`cv_auc`/`brier`の算出の両方に使っている（二重利用）。これらは**リーク・楽観化の方向に働くコード上の事実**であり、それ自体は本分析とは独立に確認できる。ただし「だから本番の0.52は今回の0.50よりどれだけ楽観的か」という**差の大きさは今回の分析からは主張できない**。

---

## 4. 3戦略比較（是正版：独立3期間 + 対照実験）

### 是正1（本番実効設定）反映後の実行条件

- `initial_capital` = 500,000円
- `buy_threshold` = **0.08**（risk_profile適用後。対照実験のみ0.50に上書き）
- `rule_weight` = 0.5、`ml_weight` = 0.5（risk_profileの対象キーではないため`config.yaml`の値のまま）
- `sizing` = `SizingConfig(max_position_ratio=0.50, max_positions=8, max_sector_ratio=0.55)`
- `liquidity` = `LiquidityConfig(max_volume_share=0.0)`（既定値のまま）
- `retrain` = `RetrainConfig(every_sessions=20, warmup_sessions=120)`
- 学習データは是正後のイベント表（`events_and_ohlcv_v2.pkl`、12,033件）を使用

### 是正3: 独立な非重複3期間

前回の「直近1年」「直近2年」は入れ子で独立な確認になっていなかった。今回は重複しない3つの1年間で比較した:

- 期間A: 2023-09-18 〜 2024-09-18（246セッション）
- 期間B: 2024-09-18 〜 2025-09-18（245セッション）
- 期間C: 2025-09-18 〜 2026-09-18（245セッション、前回の「直近1年」と同じ暦区間だが、risk_profile適用後の値で測り直した）

### 是正4: 対照実験（rule_only@0.50）を追加した結果表

`rule_only@0.50`は、本番実効の`buy_threshold=0.08`のかわりに`0.50`（前回レビューが実データで確認した、weighted_blendの実効ゲート相当の厳しさ）だけを上書きし、他は全て本番実効のまま実行したもの。

#### 期間A（2023-09-18〜2024-09-18、246セッション）

| 戦略 | 最終NAV | 総リターン | 最大DD | シャープ | トレード数 | 実行時間 |
|---|---|---|---|---|---|---|
| rule_only（buy_threshold=0.08） | 513,890円 | +2.78% | -11.32% | 0.251 | 199 | 37.8秒 |
| **rule_only@0.50（対照実験）** | **572,892円** | **+14.58%** | -8.51% | **1.104** | 53 | 122.1秒 |
| weighted_blend | 495,816円 | -0.84% | -15.88% | 0.027 | 193 | 144.1秒 |
| rule_then_ml | 467,428円 | -6.51% | -17.29% | -0.393 | 198 | 439.2秒 |

→ **この期間の最良戦略はrule_only@0.50（対照実験）。weighted_blendはrule_only（0.08）にも劣り、対照実験には大差で劣る。**

#### 期間B（2024-09-18〜2025-09-18、245セッション）

| 戦略 | 最終NAV | 総リターン | 最大DD | シャープ | トレード数 | 実行時間 |
|---|---|---|---|---|---|---|
| rule_only（buy_threshold=0.08） | 528,827円 | +5.77% | -8.81% | 0.490 | 194 | 30.5秒 |
| rule_only@0.50（対照実験） | 551,610円 | +10.32% | -10.88% | 0.931 | 49 | 30.8秒 |
| **weighted_blend** | **565,610円** | **+13.12%** | -7.58% | **0.933** | 187 | 115.0秒 |
| rule_then_ml | 531,581円 | +6.32% | -9.64% | 0.518 | 192 | 150.9秒 |

→ **この期間はweighted_blendが最良**（リターンで対照実験をわずかに上回る。シャープはほぼ同値0.933 vs 0.931）。

#### 期間C（2025-09-18〜2026-09-18、245セッション）

| 戦略 | 最終NAV | 総リターン | 最大DD | シャープ | トレード数 | 実行時間 |
|---|---|---|---|---|---|---|
| **rule_only（buy_threshold=0.08）** | **503,709円** | **+0.74%** | -7.42% | **0.124** | 174 | 14.3秒 |
| rule_only@0.50（対照実験） | 493,418円 | -1.32% | -5.73% | -0.079 | 43 | 32.5秒 |
| weighted_blend | 409,125円 | -18.18% | -20.39% | -1.580 | 154 | 124.8秒 |
| rule_then_ml | 475,234円 | -4.95% | -9.11% | -0.357 | 167 | 155.8秒 |

→ **この期間は本番実効そのままのrule_only（0.08）が最良。weighted_blendは4パターン中最下位（-18.18%、最大DDも-20.39%と突出して悪い）。**

全実行で`degraded=False`（再学習・推論とも例外なく完走）。

### 是正3・4への回答（3期間中の優劣まとめ）

| 比較基準 | weighted_blendが優位だった期間数 |
|---|---|
| rule_only（0.08、本番実効ゲート）に対して | **1/3期間**（Bのみ。A・Cでは劣る） |
| rule_only@0.50（対照実験、厳しいゲート）に対して | **1/3期間**（Bのみ、僅差。A・Cでは大差で劣る） |

**前回報告書の「両期間ともweighted_blendが最も優れている」は誤りだったことが独立3期間の検証で判明した。** また是正4の対照実験により、**唯一weighted_blendが優位だった期間Bでも、その優位性はrule_only@0.50（対照実験）に対してはリターンで+2.8pt・シャープでほぼ同値という僅差**であり、「MLが効いている」と言うより「候補を絞るゲートの厳しさ」の効果と実質的に区別が付かない。3期間中2期間（A・C）では対照実験にすら大差で負けており、**weighted_blendのML項が収益に体系的に寄与しているという根拠は今回の是正後データからは得られなかった。**

### 是正5: ウォームアップ期間の影響

`retrain_warmup_sessions=120`。各期間とも約245〜246セッションのため、ウォームアップ比率はほぼ同一:

| 期間 | 総セッション数 | ウォームアップセッション数 | ウォームアップ比率 |
|---|---|---|---|
| A | 246 | 120 | 48.8% |
| B | 245 | 120 | 49.0% |
| C | 245 | 120 | 49.0% |

**各期間とも約49%がウォームアップ期間であり、この間`model_holder`は空でMLは常に`None`（`weighted_blend`/`rule_then_ml`とも実質的に`rule_only`と同じ動きしかしていない）。** つまり上記の期間別成績も、実際にMLが機能していたのは各期間の後半約半分だけであり、この分析の枠組み（時間の制約）ではウォームアップ後だけを切り出した別集計までは行っていない（ブリーフの「必須ではない」規定に従い今回は見送った）。**この約49%という比率自体が、`retrain_warmup_sessions=120`を1年間のバックテストに対して使う設計の限界を示している**（1年間の約半分でしかMLの寄与を測れない）。

---

## 5. 是正7: purge/embargoの記述訂正

`validation.split_events()`は`embargo_sessions`パラメータを持つが（既定0）、`evaluate_fold`/`fit_inner`（`src/strategy/evaluation.py`）はこのパラメータを渡していない。`run_evaluation()`のソースにも`embargo`という文字列は一切登場しない。**よって今回の5モデル比較は「purge付き分割（embargoは今回0を使用）」であり、「purge/embargo付き分割」ではない。** 前回報告書の記述はこの点で不正確だった。

---

## 6. 実行時間まとめ

| 処理 | 内容 | 所要時間 |
|---|---|---|
| 段階2(v2) | risk_profile適用後のイベント表再構築（OHLCVは前回のものを再利用） | 135.1秒 |
| 段階3(v2) | 5モデル比較 `run_evaluation`（n_splits=5, persist=False, 統計処理込み） | 609.6秒 |
| 段階4(v2)-A | 期間A: rule_only/rule_only@0.50/weighted_blend/rule_then_ml | 37.8+122.1+144.1+439.2 = 743.2秒 |
| 段階4(v2)-B | 期間B: 同上4パターン | 30.5+30.8+115.0+150.9 = 327.2秒 |
| 段階4(v2)-C | 期間C: 同上4パターン | 14.3+32.5+124.8+155.8 = 327.4秒 |

合計: 約2,142.5秒（約35.7分）。前回の分析結果（イベント構築前の隔離DB構築・OHLCV読込）はそのまま再利用したため、今回はゼロから再構築していない。

---

## 7. 所感 — 前回の推奨の取り下げと更新

**前回報告書の推奨（`ml_weight`維持・weighted_blend優位を根拠にした示唆）は撤回する。** 是正後の独立3期間比較・対照実験により、weighted_blendが一貫して優れているという根拠は失われた。

1. **5モデル比較（是正後）**: current_lightgbm（現行相当）のAUCは0.5018で、0.5との統計的有意差なし（p=0.72）。「予測力がある」とも「chance未満」とも言えず、**予測力の有無自体が確認できていない**、というのが正確な結論。
2. **3戦略比較（是正後）**: weighted_blendが本番実効ゲート（rule_only@0.08）にもゲートの厳しさを揃えた対照実験（rule_only@0.50）にも優位だったのは3期間中1期間のみ。残り2期間（A・C）では**weighted_blendが4パターン中最下位または劣位**であり、特に期間Cでは総リターン-18.18%・最大DD-20.39%と突出して悪い。
3. **ウォームアップ期間が各期間の約49%を占める**ため、そもそもMLが機能していた期間は評価対象の半分程度しかない。

### 推奨（実際の設定変更はユーザーの判断を仰ぐ）

- **`ml_weight: 0.5`を維持する根拠も、下げる/上げる根拠も、今回の分析だけでは十分に得られていない。** 前回のような「weighted_blendが一貫して優れているのでフィルター効果を疑うべき」という主張は撤回する。
- **むしろ現状のデータからは「weighted_blendがrule_onlyより悪化する期間がある」ことの方が明確なシグナル**であり、これは実運用中のリスクとして扱う価値がある（特に期間Cは直近1年＝現在の本番運用に最も近い期間で、weighted_blendが最下位だった点は要注意）。
- 追加で検討する価値がある次の一手:
  1. 3期間の外側にもう1〜2期間（例えば2021-2022年、2022-2023年）を追加して、weighted_blendの優劣がさらにどう分布するかを確認する。3期間では判断material としてまだ少ない。
  2. `retrain_warmup_sessions=120`が1年間のバックテストの約半分を占める点を踏まえ、より長い評価期間（例: 3年連続）でウォームアップ後だけを集計する分析を追加する。
  3. `engine_version: v2`への切替検討自体は、walk-forwardエンジンの設計面（purge分割・T+1約定・出来高制約・複数銘柄資金競合等）がlegacyより正確であることを理由に引き続き検討する価値があるが、これはML予測力やweighted_blendの優劣とは別軸の論点として扱うべき。
  4. 本番`model_metrics`のCV手法（銘柄連結行へのTimeSeriesSplit・early stoppingとの二重利用）はコード上明白な楽観化要因であり、**是正後の測定と無関係に**改善の価値がある（8.\は本分析の範囲外）。

**設定変更（config.yaml）は一切行っていない。上記はすべて分析に基づく推奨であり、実際の変更はユーザーの判断を仰ぐ。**

---

## 8. 本番資産が汚れていないことの確認

| 項目 | 開始前 | 終了後 | 判定 |
|---|---|---|---|
| `data/kabu_auto.db` size/mtime | 11,677,696 bytes / 1789887747.77 | 同一 | 変化なし |
| `data/kabu_auto.db-wal` size/mtime | 4,297,192 bytes / 1789887919.72 | 同一 | 変化なし |
| `data/kabu_auto.db-shm` size/mtime | 32,768 bytes / 1789869110.80 | 同一 | 変化なし |
| `config.yaml` size/mtime | 22,819 bytes / 1789868261.39 | 同一 | 変化なし |
| `risk_profile.json` size/mtime | 205 bytes / 1784530004.54 | 同一 | 変化なし |
| `watchlists.json` size/mtime | 9,609 bytes / 1789811262.67 | 同一 | 変化なし |
| `models/lgb_model.pkl` / `.meta.json` mtime | 1789697690.33 / 1789697690.35 | 同一 | 変化なし |
| `git status --porcelain`（リポジトリ全体） | クリーン | クリーン | 差分なし |

### 隔離手順の要点（前回から踏襲、変更なし）

- 本番DBは**読み取り専用URI接続**（`mode=ro`）でのみ開く。書き込みは一時DB（スクラッチパッド配下）に対してのみ。
- `config.yaml`・`risk_profile.json`は**読むだけ**（`risk_profile.load()`は`_persist()`を呼ばないことをソースで確認済み。§1参照）。
- `evaluation.run_evaluation()`は`persist=False`で実行（`predictions`/`evaluation_runs`/`prediction_outcomes`への書き込みなし）。
- `walkforward.run_walkforward()`の結果は`wf.save_run()`を一度も呼ばず、`BacktestRun`/`RunModelUsage`テーブルへの保存なし（結果はスクラッチパッド配下のpickleにのみ保存）。
- 学習した`CurrentLightGBM`系モデルはメモリ上でのみ使用し、`model_store.save_candidate()`等は一切呼んでいない。
- ネットワークアクセスなし。本番プロセス（ポート8080・liveモード）の停止・再起動は行っていない。

**結論: 本番資産（config.yaml・risk_profile.json・data/kabu_auto.db・models/）への書き込みは一切無し。**

---
---

# 追記: 独立期間を3→6に拡張した分析（2026-09-21）

上記（v2・独立3期間A/B/C: 2023-2026）は「3期間では判断材料としてまだ
少ない」という限界が残っていた。本番DBには2019-06-19まで遡るOHLCVが
既にあるため、**待たずに今すぐ使える過去データ（2019-2023）で独立期間を
3つ追加**した（期間D/E/F、加えて助走期間の制約付きで参考期間Gも実施）。

**イベント表・OHLCVの再構築、5モデル比較のやり直しは行っていない。**
追加したのは3戦略比較（walkforward）の期間だけ。前回（v2）と全く同じ
`policy_conf`/`costs`/`sizing`/`liquidity`/`retrain`設定・同じ
risk_profile（high_risk）適用手順を踏襲した。

## 追加期間の選定とfeature_valid助走期間の実測

`events_and_ohlcv_v2.pkl`のOHLCV日付範囲（2019-06-19〜2026-09-18）を
確認し、`build_feature_frame`を62銘柄全てに実行して助走期間の実測を
行った上で、次の独立な非重複1年期間を追加した:

- 期間D: 2022-09-18 〜 2023-09-18
- 期間E: 2021-09-18 〜 2022-09-18
- 期間F: 2020-09-18 〜 2021-09-18
- 期間G（参考、COVIDショック含む）: 2019-09-18 〜 2020-09-18
  — **開始時点で62銘柄すべてがfeature_valid=False**（最速の銘柄でも
  2019-10-07まで助走期間が続く）で、冒頭約3週間は実質取引不可能な
  期間を含む。本編の統計には含めず参考値として別掲する。

期間D/E/Fはいずれも開始時点で61/62銘柄がfeature_valid（新規上場1銘柄を
除き問題なし）。

## 各追加期間の4パターン結果（実測値）

全実行で`degraded=False`（再学習・推論とも例外なく完走）。

### 期間D（2022-09-18〜2023-09-18、245セッション）

| 戦略 | 最終NAV | 総リターン | 最大DD | シャープ | トレード数 |
|---|---|---|---|---|---|
| rule_only（buy_threshold=0.08） | 581,332円 | +16.27% | -5.46% | 1.144 | 236 |
| rule_only@0.50（対照実験） | 488,541円 | -2.29% | -8.42% | -0.204 | 51 |
| **weighted_blend** | **696,616円** | **+39.32%** | -6.31% | **2.197** | 238 |
| rule_then_ml | 610,012円 | +22.00% | -6.25% | 1.534 | 236 |

→ weighted_blendが4パターン中最良。rule_only（0.08）にもcontrol050にも大差で優位。

### 期間E（2021-09-18〜2022-09-18、244セッション）

| 戦略 | 最終NAV | 総リターン | 最大DD | シャープ | トレード数 |
|---|---|---|---|---|---|
| **rule_only（buy_threshold=0.08）** | **474,436円** | **-5.11%** | -16.18% | **-0.275** | 255 |
| rule_only@0.50（対照実験） | 494,649円 | -1.07% | -17.33% | -0.013 | 72 |
| weighted_blend | 444,668円 | -11.07% | -20.46% | -0.694 | 229 |
| rule_then_ml | 466,722円 | -6.66% | -16.18% | -0.370 | 242 |

→ 4パターン全てがマイナス。weighted_blendが4パターン中最下位（最大DDも最悪）。

### 期間F（2020-09-18〜2021-09-18、244セッション）

| 戦略 | 最終NAV | 総リターン | 最大DD | シャープ | トレード数 |
|---|---|---|---|---|---|
| rule_only（buy_threshold=0.08） | 658,853円 | +31.77% | -6.22% | 1.967 | 265 |
| rule_only@0.50（対照実験） | 517,122円 | +3.42% | -6.95% | 0.391 | 52 |
| **weighted_blend** | **684,100円** | **+36.82%** | -6.05% | 2.060 | 258 |
| rule_then_ml | 670,333円 | +34.07% | -4.83% | **2.104**（シャープのみ最良） | 269 |

→ weighted_blendが総リターン最良（シャープはrule_then_mlがわずかに上回るが僅差）。

### 期間G（参考、2019-09-18〜2020-09-18、244セッション、COVIDショック含む・助走期間不足あり）

| 戦略 | 最終NAV | 総リターン | 最大DD | シャープ | トレード数 |
|---|---|---|---|---|---|
| rule_only（buy_threshold=0.08） | 475,653円 | -4.87% | -35.49% | -0.095 | 276 |
| **rule_only@0.50（対照実験）** | 404,266円 | **-19.15%（最悪）** | -30.39% | **-1.141（最悪）** | 71 |
| weighted_blend | 435,024円 | -13.00% | -32.75% | -0.593 | 303 |
| rule_then_ml | 438,292円 | -12.34% | -35.49% | -0.561 | 275 |

→ 参考値。最大DDが4パターンとも-30%台〜-35%台とA-F期間よりはるかに悪く、
COVIDショックの影響が明確。助走期間不足（冒頭3週間0/62）の影響で
母集団が他期間より不安定な可能性があり、本編の統計には含めない。

## ウォームアップ期間比率（追加期間でも同様）

`retrain_warmup_sessions=120`は期間D/E/F/Gいずれも約49%を占め、前回
（A/B/C: 48.8%〜49.0%）とほぼ同一。**評価対象の約半分の期間しかMLが
機能していない**という限界は追加期間でも変わらない。

## 独立6期間（A-F）全体のまとめ

### weighted_blendの優劣（期間別）

| 期間 | vs rule_only(0.08) | vs control050（対照実験） |
|---|---|---|
| A（2023-2024） | 劣位 | 劣位 |
| B（2024-2025） | 優位（僅差） | 優位（僅差） |
| C（2025-2026） | 劣位 | 劣位 |
| D（2022-2023、新規） | **優位（大差）** | **優位（大差）** |
| E（2021-2022、新規） | 劣位 | 劣位 |
| F（2020-2021、新規） | **優位** | **優位** |

**独立6期間中、weighted_blendが優位だったのは3/6期間（B, D, F）**
（rule_only(0.08)・control050のどちらに対しても同じ3/6）。

### 平均リターン・標準偏差（独立6期間A-F）

| 戦略 | 平均リターン | 標準偏差 |
|---|---|---|
| rule_only（0.08） | +8.70% | 13.32% |
| rule_only@0.50（対照実験） | +3.94% | 7.01% |
| weighted_blend | +9.86% | **24.26%** |
| rule_then_ml | +7.38% | 17.14% |

**weighted_blendは平均リターンでは僅かに最高値だが、標準偏差が他の
3パターンの1.4〜3.5倍と突出して大きい**（ばらつきが大きい）。

### 対応のあるt検定（scipy.stats.ttest_rel、独立6期間A-F）

| 比較 | n | 平均差 | t | p |
|---|---|---|---|---|
| weighted_blend − rule_only(0.08) | 6 | +1.16pt | 0.200 | **0.8490** |
| weighted_blend − control050 | 6 | +5.92pt | 0.568 | **0.5948** |

**いずれも統計的に有意ではない。** n=6は検定力が極めて低いサンプルサイズ
であり、「有意差なし」は「差が無いことの証明」ではなく「この程度の
サンプル数では判別できない」という意味に留まる。

## 結論の更新: 前回（3期間）の結論はより強く裏付けられた

**結論の骨子は変わらない。むしろ独立期間を3→6に増やしたことで、
「weighted_blendが一貫して優れているという根拠はない」という結論が
より強く裏付けられた。**

- 優位期間の「割合」自体は1/3→3/6（50%）まで上昇したが、これは
  「優れている」ことの証明にはならない: (1) t検定で有意差なし
  (p=0.85, p=0.59)、(2) weighted_blendの標準偏差が突出しており、
  D・F期間の大勝ちがA・C・E期間の負けを相殺して平均を押し上げて
  いるだけの可能性が高い。
- **「weighted_blendは一部の期間で大きく勝つが、それ以上に別の期間で
  大きく負けることがある」というリスク像が、前回より明確になった。**
- 興味深い点として、D・F期間（weighted_blend優位）ではcontrol050
  （ゲートを厳しくしただけの対照実験）はむしろ悪化・停滞していた
  （D: -2.29%、F: +3.42%）。この2期間ではゲートの厳しさだけでは
  説明できず、MLが銘柄選別に何らかの寄与をした可能性が示唆される
  一方、E・C期間ではMLがマイナスに寄与しているように見える。
  **この非一貫性自体が「weighted_blendの効果は相場のレジームに
  強く依存し、一律の設定変更を正当化する根拠にはならない」という
  結論を補強している。**

### 推奨（更新なし。設定変更は一切行っていない）

`ml_weight`を維持する根拠も変更する根拠も、6期間に拡張した分析でも
十分には得られなかった。追加で検討する価値がある次の一手は変わらず:
①ウォームアップ後だけを切り出した集計 ②本番`model_metrics`のCV手法
自体の改善（`ml_model.py`の銘柄連結TimeSeriesSplit・early stoppingとの
二重利用） ③`engine_version: v2`への切替検討はML予測力の優劣とは
別軸（walk-forwardエンジンの設計面）で引き続き価値がある。

## 実行時間・本番資産の確認

追加期間D+E+F（本編）の実行合計: 約9.5分。D+E+F+G（参考含む）: 約12.6分。
本セッション全体（事前確認・集計含む）: 約35分。

本番資産（config.yaml・risk_profile.json・data/kabu_auto.db（-wal/-shm）・
models/）のsize/mtimeは、分析開始前・終了後で完全一致（前回v2記載値とも
一致）。`git status --porcelain`もクリーン。**書き込みは一切無し。**
